<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:css='false' b:defaultwidgetversion='2' b:layoutsVersion='3' b:responsive='true' b:templateUrl='indie' b:templateVersion='1.0.0' expr:dir='data:blog.languageDirection' expr:lang='data:blog.locale'>
  <head>
    <meta charset='UTF-8'/>
    <meta content='width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=5.0' name='viewport'/>
    <title><data:blog.pageTitle/></title>
    
    <b:if cond='data:blog.metaDescription'>
      <meta expr:content='data:blog.metaDescription' name='description'/>
    </b:if>
    
    <b:skin version='1.0.0'><![CDATA[
    /*
    =====================================================
    Theme Name: Modern Portfolio
    Description: A minimal, professional portfolio theme
    Version: 1.0.0
    Author: Blogger Theme Developer
    =====================================================
    */
    
    /* CSS Variables - Modern Tech Portfolio */
    :root {
      /* Colors */
      --primary-color: #2563eb;
      --secondary-color: #7c3aed;
      --accent-color: #10b981;
      --text-primary: #1f2937;
      --text-secondary: #6b7280;
      --text-light: #9ca3af;
      --bg-primary: #ffffff;
      --bg-secondary: #f9fafb;
      --bg-dark: #111827;
      --border-color: #e5e7eb;
      --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
      --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
      --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
      --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
      
      /* Typography */
      --font-primary: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      --font-secondary: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      --font-mono: 'Fira Code', 'Courier New', monospace;
      
      /* Font Sizes */
      --text-xs: 0.75rem;
      --text-sm: 0.875rem;
      --text-base: 1rem;
      --text-lg: 1.125rem;
      --text-xl: 1.25rem;
      --text-2xl: 1.5rem;
      --text-3xl: 1.875rem;
      --text-4xl: 2.25rem;
      --text-5xl: 3rem;
      --text-6xl: 3.75rem;
      
      /* Spacing */
      --spacing-xs: 0.5rem;
      --spacing-sm: 0.75rem;
      --spacing-md: 1rem;
      --spacing-lg: 1.5rem;
      --spacing-xl: 2rem;
      --spacing-2xl: 3rem;
      --spacing-3xl: 4rem;
      
      /* Layout */
      --max-width: 1280px;
      --border-radius: 0.5rem;
      --border-radius-lg: 1rem;
      --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    /* Global Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    /* Base Styles */
    html {
      font-size: 16px;
      scroll-behavior: smooth;
    }
    
    body {
      font-family: var(--font-primary);
      color: var(--text-primary);
      background-color: var(--bg-primary);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
    
    /* Container */
    .container {
      max-width: var(--max-width);
      margin: 0 auto;
      padding: 0 var(--spacing-lg);
    }
    
    /* Section Spacing */
    .section {
      padding: var(--spacing-3xl) 0;
    }
    
    /* Typography */
    h1, h2, h3, h4, h5, h6 {
      font-weight: 700;
      line-height: 1.2;
      color: var(--text-primary);
    }
    
    h1 {
      font-size: var(--text-5xl);
    }
    
    h2 {
      font-size: var(--text-4xl);
    }
    
    h3 {
      font-size: var(--text-3xl);
    }
    
    a {
      color: var(--primary-color);
      text-decoration: none;
      transition: var(--transition);
    }
    
    a:hover {
      color: var(--secondary-color);
    }
    
    /* Utility Classes */
    .text-center {
      text-align: center;
    }
    
    .bg-secondary {
      background-color: var(--bg-secondary);
    }
    
    .bg-dark {
      background-color: var(--bg-dark);
      color: #ffffff;
    }
    
    /* Section Widgets (when empty) */
    .section .widget {
      margin: var(--spacing-lg) 0;
    }
    
    /* Responsive */
    @media (max-width: 768px) {
      :root {
        --text-5xl: 2.5rem;
        --text-4xl: 2rem;
        --text-3xl: 1.5rem;
        --spacing-3xl: 2rem;
      }
      
      .container {
        padding: 0 var(--spacing-md);
      }
    }
    
    /* Default Widget Styles */
    .widget {
      margin-bottom: var(--spacing-lg);
    }
    
    .widget-title {
      font-size: var(--text-xl);
      font-weight: 600;
      margin-bottom: var(--spacing-md);
    }
    
    /* Blog Section Styles (Case Studies) */
    .blog-container {
      padding: var(--spacing-3xl) 0;
    }
    
    /* Posts Grid */
    .posts-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
      gap: var(--spacing-xl);
      margin-bottom: var(--spacing-2xl);
    }
    
    /* Post Card */
    .post-card {
      background: var(--bg-primary);
      border-radius: var(--border-radius-lg);
      overflow: hidden;
      transition: var(--transition);
      box-shadow: var(--shadow-sm);
      display: flex;
      flex-direction: column;
      height: 100%;
    }
    
    .post-card:hover {
      transform: translateY(-8px);
      box-shadow: var(--shadow-xl);
    }
    
    /* Post Thumbnail */
    .post-thumbnail {
      position: relative;
      width: 100%;
      height: 240px;
      overflow: hidden;
      background: linear-gradient(135deg, var(--bg-secondary) 0%, var(--border-color) 100%);
    }
    
    .post-thumbnail img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.5s ease;
    }
    
    .post-card:hover .post-thumbnail img {
      transform: scale(1.1);
    }
    
    .post-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, rgba(37, 99, 235, 0.8), rgba(124, 58, 237, 0.8));
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
      transition: opacity 0.3s ease;
    }
    
    .post-card:hover .post-overlay {
      opacity: 1;
    }
    
    .overlay-icon {
      transform: scale(0.8);
      transition: transform 0.3s ease;
    }
    
    .post-card:hover .overlay-icon {
      transform: scale(1);
    }
    
    /* Post Content */
    .post-content {
      padding: var(--spacing-xl);
      display: flex;
      flex-direction: column;
      flex-grow: 1;
    }
    
    /* Post Labels */
    .post-labels {
      display: flex;
      gap: var(--spacing-xs);
      margin-bottom: var(--spacing-md);
      flex-wrap: wrap;
    }
    
    .post-label {
      display: inline-block;
      padding: calc(var(--spacing-xs) / 2) var(--spacing-sm);
      background: linear-gradient(135deg, rgba(37, 99, 235, 0.1), rgba(124, 58, 237, 0.1));
      color: var(--primary-color);
      font-size: var(--text-xs);
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      border-radius: 4px;
      text-decoration: none;
      transition: var(--transition);
    }
    
    .post-label:hover {
      background: var(--primary-color);
      color: white;
    }
    
    /* Post Title */
    .post-title {
      font-size: var(--text-xl);
      font-weight: 700;
      line-height: 1.3;
      margin-bottom: var(--spacing-md);
    }
    
    .post-title a {
      color: var(--text-primary);
      text-decoration: none;
      transition: var(--transition);
    }
    
    .post-title a:hover {
      color: var(--primary-color);
    }
    
    /* Post Snippet */
    .post-snippet {
      font-size: var(--text-base);
      color: var(--text-secondary);
      line-height: 1.7;
      margin-bottom: var(--spacing-lg);
      flex-grow: 1;
      display: -webkit-box;
      -webkit-line-clamp: 3;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    
    /* Post Footer */
    .post-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-top: var(--spacing-md);
      border-top: 1px solid var(--border-color);
      gap: var(--spacing-md);
    }
    
    .read-more-btn {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-xs);
      padding: var(--spacing-sm) var(--spacing-lg);
      background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
      color: white;
      font-size: var(--text-sm);
      font-weight: 600;
      border-radius: var(--border-radius);
      text-decoration: none;
      transition: var(--transition);
    }
    
    .read-more-btn:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
      color: white;
    }
    
    .read-more-btn svg {
      transition: transform 0.3s ease;
    }
    
    .read-more-btn:hover svg {
      transform: translateX(4px);
    }
    
    .post-meta {
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
      font-size: var(--text-xs);
      color: var(--text-secondary);
    }
    
    /* Blog Pagination */
    .blog-pagination {
      display: flex;
      justify-content: center;
      gap: var(--spacing-md);
      margin-top: var(--spacing-3xl);
    }
    
    .pagination-btn {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-sm);
      padding: var(--spacing-md) var(--spacing-xl);
      background: var(--bg-primary);
      color: var(--primary-color);
      font-size: var(--text-base);
      font-weight: 600;
      border: 2px solid var(--primary-color);
      border-radius: var(--border-radius);
      text-decoration: none;
      transition: var(--transition);
    }
    
    .pagination-btn:hover {
      background: var(--primary-color);
      color: white;
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }
    
    /* Responsive Blog Styles */
    @media (max-width: 768px) {
      .posts-grid {
        grid-template-columns: 1fr;
        gap: var(--spacing-lg);
      }
      
      .post-thumbnail {
        height: 200px;
      }
      
      .post-content {
        padding: var(--spacing-lg);
      }
      
      .post-title {
        font-size: var(--text-lg);
      }
      
      .post-snippet {
        font-size: var(--text-sm);
      }
      
      .post-footer {
        flex-direction: column;
        align-items: flex-start;
      }
      
      .read-more-btn {
        width: 100%;
        justify-content: center;
      }
      
      .blog-pagination {
        flex-direction: column;
      }
      
      .pagination-btn {
        width: 100%;
        justify-content: center;
      }
    }
    
    @media (max-width: 480px) {
      .blog-container {
        padding: var(--spacing-2xl) 0;
      }
      
      .posts-grid {
        gap: var(--spacing-md);
      }
      
      .post-thumbnail {
        height: 180px;
      }
    }
    
    ]]></b:skin>
    
    <b:template-skin><![CDATA[
    /*
    Additional Theme Styles
    Use this section for component-specific styles
    */
    ]]></b:template-skin>
    
    <b:defaultmarkups>
      <b:defaultmarkup type='Common'>
        <b:includable id='widgetNotAvailableMessage'>
          <div class='widget-not-available'>
            <p>This widget is not available in preview mode.</p>
          </div>
        </b:includable>
      </b:defaultmarkup>
    </b:defaultmarkups>
  </head>
  
  <body>
    <!-- Header Section -->
    <b:section class='header-section' id='header' maxwidgets='1' name='Header' showaddelement='yes'>
      <!-- Add Header widgets from Layout tab -->
    </b:section>
    
    <!-- Hero Area Section -->
    <b:section class='hero-section section' id='hero' maxwidgets='3' name='Hero Area' showaddelement='yes'>
      <!-- Hero Widget -->
      <b:widget id='HTML1' locked='false' title='Hero Section' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<div class="hero-container">
  <div class="container">
    <div class="hero-content">
      <div class="hero-badge">
        <span class="badge-dot"></span>
        <span class="badge-text">Available for Projects</span>
      </div>
      
      <h1 class="hero-title">
        <span class="hero-greeting">Hi, I'm</span>
        <span class="hero-name" data-editable="name">Your Name</span>
      </h1>
      
      <div class="hero-roles">
        <span class="role-item">Web Developer</span>
        <span class="role-separator">•</span>
        <span class="role-item">Founder</span>
        <span class="role-separator">•</span>
        <span class="role-item">IT Consultant</span>
      </div>
      
      <p class="hero-description" data-editable="description">
        Crafting elegant digital solutions with modern web technologies. 
        Specializing in full-stack development, cloud architecture, and transforming 
        ideas into scalable products that drive business growth.
      </p>
      
      <div class="hero-cta">
        <a href="#projects" class="btn btn-primary" data-editable="cta-primary">
          <span>View Projects</span>
          <svg class="btn-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
            <path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
        <a href="#contact" class="btn btn-secondary" data-editable="cta-secondary">
          <span>Contact Me</span>
        </a>
      </div>
      
      <div class="hero-stats">
        <div class="stat-item">
          <div class="stat-number" data-editable="years">5+</div>
          <div class="stat-label">Years Experience</div>
        </div>
        <div class="stat-divider"></div>
        <div class="stat-item">
          <div class="stat-number" data-editable="projects">50+</div>
          <div class="stat-label">Projects Completed</div>
        </div>
        <div class="stat-divider"></div>
        <div class="stat-item">
          <div class="stat-number" data-editable="clients">30+</div>
          <div class="stat-label">Happy Clients</div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
/* Hero Section Styles */
.hero-container {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--bg-primary) 0%, var(--bg-secondary) 100%);
  position: relative;
  overflow: hidden;
  padding: var(--spacing-3xl) 0;
}

.hero-container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: 
    radial-gradient(circle at 20% 50%, rgba(37, 99, 235, 0.1) 0%, transparent 50%),
    radial-gradient(circle at 80% 80%, rgba(124, 58, 237, 0.1) 0%, transparent 50%);
  pointer-events: none;
}

.hero-content {
  position: relative;
  z-index: 1;
  text-align: center;
  max-width: 900px;
  margin: 0 auto;
  animation: fadeInUp 1s ease-out;
}

/* Badge */
.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  padding: var(--spacing-xs) var(--spacing-lg);
  background: var(--bg-primary);
  border: 1px solid var(--border-color);
  border-radius: 50px;
  margin-bottom: var(--spacing-xl);
  box-shadow: var(--shadow-sm);
  animation: pulse 2s ease-in-out infinite;
}

.badge-dot {
  width: 8px;
  height: 8px;
  background: var(--accent-color);
  border-radius: 50%;
  animation: pulse-dot 2s ease-in-out infinite;
}

.badge-text {
  font-size: var(--text-sm);
  font-weight: 500;
  color: var(--text-secondary);
}

/* Title */
.hero-title {
  font-size: clamp(2.5rem, 5vw, var(--text-6xl));
  font-weight: 700;
  margin-bottom: var(--spacing-lg);
  line-height: 1.1;
}

.hero-greeting {
  display: block;
  font-size: var(--text-2xl);
  font-weight: 400;
  color: var(--text-secondary);
  margin-bottom: var(--spacing-sm);
}

.hero-name {
  display: block;
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Roles */
.hero-roles {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-xl);
  flex-wrap: wrap;
}

.role-item {
  font-size: var(--text-lg);
  font-weight: 500;
  color: var(--text-primary);
}

.role-separator {
  color: var(--primary-color);
  font-weight: 700;
}

/* Description */
.hero-description {
  font-size: var(--text-lg);
  color: var(--text-secondary);
  line-height: 1.8;
  margin-bottom: var(--spacing-2xl);
  max-width: 700px;
  margin-left: auto;
  margin-right: auto;
}

/* CTA Buttons */
.hero-cta {
  display: flex;
  gap: var(--spacing-lg);
  justify-content: center;
  margin-bottom: var(--spacing-3xl);
  flex-wrap: wrap;
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  padding: var(--spacing-md) var(--spacing-xl);
  font-size: var(--text-base);
  font-weight: 600;
  border-radius: var(--border-radius);
  transition: var(--transition);
  cursor: pointer;
  border: 2px solid transparent;
  text-decoration: none;
}

.btn-primary {
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
  color: #ffffff;
  box-shadow: var(--shadow-lg);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-xl);
  color: #ffffff;
}

.btn-secondary {
  background: var(--bg-primary);
  color: var(--text-primary);
  border-color: var(--border-color);
}

.btn-secondary:hover {
  border-color: var(--primary-color);
  color: var(--primary-color);
  transform: translateY(-2px);
}

.btn-icon {
  transition: transform 0.3s ease;
}

.btn-primary:hover .btn-icon {
  transform: translateX(4px);
}

/* Stats */
.hero-stats {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: var(--spacing-2xl);
  padding: var(--spacing-xl);
  background: var(--bg-primary);
  border-radius: var(--border-radius-lg);
  box-shadow: var(--shadow-md);
  flex-wrap: wrap;
}

.stat-item {
  text-align: center;
}

.stat-number {
  font-size: var(--text-3xl);
  font-weight: 700;
  color: var(--primary-color);
  margin-bottom: var(--spacing-xs);
}

.stat-label {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  font-weight: 500;
}

.stat-divider {
  width: 1px;
  height: 40px;
  background: var(--border-color);
}

/* Animations */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.8;
  }
}

@keyframes pulse-dot {
  0%, 100% {
    transform: scale(1);
    opacity: 1;
  }
  50% {
    transform: scale(1.1);
    opacity: 0.8;
  }
}

/* Responsive */
@media (max-width: 768px) {
  .hero-container {
    min-height: auto;
    padding: var(--spacing-2xl) 0;
  }
  
  .hero-roles {
    gap: var(--spacing-sm);
  }
  
  .role-item {
    font-size: var(--text-base);
  }
  
  .hero-description {
    font-size: var(--text-base);
  }
  
  .hero-cta {
    flex-direction: column;
    gap: var(--spacing-md);
  }
  
  .btn {
    width: 100%;
    justify-content: center;
  }
  
  .hero-stats {
    flex-direction: column;
    gap: var(--spacing-lg);
  }
  
  .stat-divider {
    display: none;
  }
}

@media (max-width: 480px) {
  .hero-badge {
    font-size: var(--text-xs);
  }
  
  .hero-greeting {
    font-size: var(--text-lg);
  }
}
</style>

<script>
// Optional: Add smooth scrolling for CTA buttons
document.addEventListener('DOMContentLoaded', function() {
  const ctaButtons = document.querySelectorAll('.hero-cta a[href^="#"]');
  
  ctaButtons.forEach(button => {
    button.addEventListener('click', function(e) {
      const href = this.getAttribute('href');
      if (href.startsWith('#')) {
        e.preventDefault();
        const target = document.querySelector(href);
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      }
    });
  });
});
</script>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- About Me Section -->
    <b:section class='about-section section bg-secondary' id='about' maxwidgets='3' name='About Me' showaddelement='yes'>
      <!-- Add About Me widgets from Layout tab -->
    </b:section>
    
    <!-- Skills Section -->
    <b:section class='skills-section section' id='skills' maxwidgets='5' name='Skills' showaddelement='yes'>
      <!-- Skills Widget -->
      <b:widget id='HTML3' locked='false' title='Technical Skills' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<div class="skills-container">
  <div class="container">
    <!-- Section Header -->
    <div class="section-header">
      <span class="section-label">Expertise</span>
      <h2 class="section-title">Technical Skills & Tools</h2>
      <p class="section-description">
        Technologies and frameworks I work with to build modern digital solutions
      </p>
    </div>
    
    <!-- Skills Categories -->
    <div class="skills-wrapper">
      
      <!-- Frontend Development -->
      <div class="skill-category">
        <h3 class="category-title">
          <svg class="category-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
            <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M2 17L12 22L22 17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M2 12L12 17L22 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          Frontend Development
        </h3>
        <div class="skills-grid" data-category="frontend">
          <span class="skill-tag" data-level="expert">Next.js</span>
          <span class="skill-tag" data-level="expert">React</span>
          <span class="skill-tag" data-level="expert">Tailwind CSS</span>
          <span class="skill-tag" data-level="advanced">TypeScript</span>
          <span class="skill-tag" data-level="advanced">JavaScript</span>
          <span class="skill-tag" data-level="advanced">HTML5/CSS3</span>
        </div>
      </div>
      
      <!-- Backend & Database -->
      <div class="skill-category">
        <h3 class="category-title">
          <svg class="category-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
            <ellipse cx="12" cy="5" rx="9" ry="3" stroke="currentColor" stroke-width="2"/>
            <path d="M21 12C21 13.66 16.97 15 12 15C7.03 15 3 13.66 3 12" stroke="currentColor" stroke-width="2"/>
            <path d="M3 5V19C3 20.66 7.03 22 12 22C16.97 22 21 20.66 21 19V5" stroke="currentColor" stroke-width="2"/>
          </svg>
          Backend & Database
        </h3>
        <div class="skills-grid" data-category="backend">
          <span class="skill-tag" data-level="expert">Prisma</span>
          <span class="skill-tag" data-level="expert">Supabase</span>
          <span class="skill-tag" data-level="advanced">Node.js</span>
          <span class="skill-tag" data-level="advanced">PostgreSQL</span>
          <span class="skill-tag" data-level="intermediate">MongoDB</span>
          <span class="skill-tag" data-level="advanced">REST APIs</span>
        </div>
      </div>
      
      <!-- Cloud & DevOps -->
      <div class="skill-category">
        <h3 class="category-title">
          <svg class="category-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
            <path d="M18 10H16.74C16.38 8.67 15.64 7.47 14.62 6.55L15.46 5.71C15.85 5.32 15.85 4.68 15.46 4.29C15.07 3.9 14.43 3.9 14.04 4.29L13.2 5.13C12.28 4.45 11.18 4 10 4C8.82 4 7.72 4.45 6.8 5.13L5.96 4.29C5.57 3.9 4.93 3.9 4.54 4.29C4.15 4.68 4.15 5.32 4.54 5.71L5.38 6.55C4.36 7.47 3.62 8.67 3.26 10H2C1.45 10 1 10.45 1 11C1 11.55 1.45 12 2 12H3.26C3.62 13.33 4.36 14.53 5.38 15.45L4.54 16.29C4.15 16.68 4.15 17.32 4.54 17.71C4.93 18.1 5.57 18.1 5.96 17.71L6.8 16.87C7.72 17.55 8.82 18 10 18C11.18 18 12.28 17.55 13.2 16.87L14.04 17.71C14.43 18.1 15.07 18.1 15.46 17.71C15.85 17.32 15.85 16.68 15.46 16.29L14.62 15.45C15.64 14.53 16.38 13.33 16.74 12H18C18.55 12 19 11.55 19 11C19 10.45 18.55 10 18 10Z" stroke="currentColor" stroke-width="2"/>
          </svg>
          Cloud & DevOps
        </h3>
        <div class="skills-grid" data-category="cloud">
          <span class="skill-tag" data-level="advanced">Vercel</span>
          <span class="skill-tag" data-level="advanced">AWS</span>
          <span class="skill-tag" data-level="intermediate">Docker</span>
          <span class="skill-tag" data-level="intermediate">CI/CD</span>
          <span class="skill-tag" data-level="advanced">Git</span>
          <span class="skill-tag" data-level="advanced">GitHub</span>
        </div>
      </div>
      
      <!-- Additional Skills -->
      <div class="skill-category">
        <h3 class="category-title">
          <svg class="category-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
            <rect x="3" y="3" width="18" height="18" rx="2" stroke="currentColor" stroke-width="2"/>
            <path d="M3 9H21" stroke="currentColor" stroke-width="2"/>
            <path d="M9 21V9" stroke="currentColor" stroke-width="2"/>
          </svg>
          Additional Skills
        </h3>
        <div class="skills-grid" data-category="additional">
          <span class="skill-tag" data-level="expert">Web Development</span>
          <span class="skill-tag" data-level="advanced">UI/UX Design</span>
          <span class="skill-tag" data-level="advanced">Responsive Design</span>
          <span class="skill-tag" data-level="intermediate">SEO</span>
          <span class="skill-tag" data-level="advanced">API Integration</span>
          <span class="skill-tag" data-level="intermediate">Testing</span>
        </div>
      </div>
      
    </div>
    
    <!-- Skills Footer Note -->
    <div class="skills-footer">
      <p class="skills-note">
        💡 <strong>Always Learning:</strong> Continuously exploring new technologies and best practices
      </p>
    </div>
  </div>
</div>

<style>
/* Skills Section Styles */
.skills-container {
  padding: var(--spacing-3xl) 0;
}

/* Skills Wrapper */
.skills-wrapper {
  display: grid;
  gap: var(--spacing-2xl);
  margin-bottom: var(--spacing-2xl);
}

/* Skill Category */
.skill-category {
  background: var(--bg-primary);
  border-radius: var(--border-radius-lg);
  padding: var(--spacing-xl);
  box-shadow: var(--shadow-sm);
  transition: var(--transition);
  animation: fadeInUp 0.8s ease-out backwards;
}

.skill-category:nth-child(1) { animation-delay: 0.1s; }
.skill-category:nth-child(2) { animation-delay: 0.2s; }
.skill-category:nth-child(3) { animation-delay: 0.3s; }
.skill-category:nth-child(4) { animation-delay: 0.4s; }

.skill-category:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-2px);
}

/* Category Title */
.category-title {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  font-size: var(--text-xl);
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: var(--spacing-lg);
}

.category-icon {
  color: var(--primary-color);
  flex-shrink: 0;
}

/* Skills Grid */
.skills-grid {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-sm);
}

/* Skill Tag - Modern Pills */
.skill-tag {
  position: relative;
  display: inline-flex;
  align-items: center;
  padding: var(--spacing-sm) var(--spacing-lg);
  background: var(--bg-secondary);
  color: var(--text-primary);
  font-size: var(--text-sm);
  font-weight: 500;
  border-radius: 50px;
  border: 2px solid transparent;
  transition: var(--transition);
  cursor: default;
  overflow: hidden;
}

/* Skill Level Indicators */
.skill-tag[data-level="expert"]::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 3px;
  background: linear-gradient(90deg, var(--accent-color), var(--primary-color));
}

.skill-tag[data-level="advanced"]::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 75%;
  height: 3px;
  background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
}

.skill-tag[data-level="intermediate"]::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 50%;
  height: 3px;
  background: linear-gradient(90deg, var(--secondary-color), var(--accent-color));
}

/* Hover Effects */
.skill-tag:hover {
  background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
  color: white;
  border-color: var(--primary-color);
  transform: translateY(-2px) scale(1.05);
  box-shadow: var(--shadow-md);
}

/* Expert Level - Extra Glow */
.skill-tag[data-level="expert"]:hover {
  box-shadow: 0 0 20px rgba(16, 185, 129, 0.4);
}

/* Skills Footer */
.skills-footer {
  text-align: center;
  padding-top: var(--spacing-xl);
  border-top: 2px dashed var(--border-color);
}

.skills-note {
  font-size: var(--text-base);
  color: var(--text-secondary);
  line-height: 1.6;
}

.skills-note strong {
  color: var(--primary-color);
}

/* Responsive Design */
@media (max-width: 768px) {
  .skill-category {
    padding: var(--spacing-lg);
  }
  
  .category-title {
    font-size: var(--text-lg);
  }
  
  .skill-tag {
    font-size: var(--text-xs);
    padding: calc(var(--spacing-xs) + 2px) var(--spacing-md);
  }
  
  .skills-grid {
    gap: var(--spacing-xs);
  }
}

@media (max-width: 480px) {
  .skills-container {
    padding: var(--spacing-2xl) 0;
  }
  
  .skills-wrapper {
    gap: var(--spacing-lg);
  }
  
  .category-icon {
    width: 20px;
    height: 20px;
  }
  
  .skill-tag {
    padding: var(--spacing-xs) var(--spacing-sm);
  }
}

/* Animation */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Print Styles */
@media print {
  .skill-tag {
    border: 1px solid var(--border-color);
    background: white !important;
    color: var(--text-primary) !important;
  }
}
</style>

<script>
// Optional: Add skill tag interactions
document.addEventListener('DOMContentLoaded', function() {
  const skillTags = document.querySelectorAll('.skill-tag');
  
  // Add click event for potential skill detail modal or tooltip
  skillTags.forEach(tag => {
    tag.addEventListener('click', function() {
      const skillName = this.textContent.trim();
      const skillLevel = this.getAttribute('data-level');
      
      // You can add custom functionality here
      // For example: show a tooltip, open a modal, or log analytics
      console.log(`Skill: ${skillName}, Level: ${skillLevel}`);
    });
    
    // Add hover effect enhancement
    tag.addEventListener('mouseenter', function() {
      const level = this.getAttribute('data-level');
      if (level === 'expert') {
        this.style.animationPlayState = 'running';
      }
    });
  });
  
  // Animate skill tags on scroll (optional)
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -100px 0px'
  };
  
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.animation = 'fadeInUp 0.5s ease-out';
      }
    });
  }, observerOptions);
  
  document.querySelectorAll('.skill-category').forEach(category => {
    observer.observe(category);
  });
});
</script>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- Main Blog Section (Case Studies) -->
    <b:section class='main-section section bg-secondary' id='main' maxwidgets='1' name='Blog Posts (Case Studies)' showaddelement='no'>
      <!-- Blog Widget -->
      <b:widget id='Blog1' locked='true' title='Case Studies' type='Blog' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='showTitle'>true</b:widget-setting>
          <b:widget-setting name='showPostFooter'>false</b:widget-setting>
        </b:widget-settings>
        
        <!-- Main Blog Container -->
        <b:includable id='main' var='this'>
          <div class='blog-container'>
            <div class='container'>
              <!-- Section Header -->
              <div class='section-header'>
                <span class='section-label'>Latest Work</span>
                <h2 class='section-title'>Case Studies &amp; Articles</h2>
                <p class='section-description'>
                  Detailed insights into projects, technical deep-dives, and industry perspectives
                </p>
              </div>
              
              <!-- Posts Grid -->
              <div class='posts-grid'>
                <b:loop values='data:posts' var='post'>
                  <b:include name='post' data='post'/>
                </b:loop>
              </div>
              
              <!-- Pagination -->
              <b:include name='postPagination'/>
            </div>
          </div>
        </b:includable>
        
        <!-- Individual Post Card -->
        <b:includable id='post' var='post'>
          <article class='post-card' expr:data-post-id='data:post.id'>
            <!-- Post Thumbnail -->
            <b:if cond='data:post.firstImageUrl'>
              <div class='post-thumbnail'>
                <a expr:href='data:post.url' expr:title='data:post.title'>
                  <img expr:alt='data:post.title' expr:src='data:post.firstImageUrl' loading='lazy'/>
                  <div class='post-overlay'>
                    <svg class='overlay-icon' width='48' height='48' viewBox='0 0 48 48' fill='none'>
                      <circle cx='24' cy='24' r='24' fill='rgba(255,255,255,0.2)'/>
                      <path d='M19 14L29 24L19 34' stroke='white' stroke-width='3' stroke-linecap='round' stroke-linejoin='round'/>
                    </svg>
                  </div>
                </a>
              </div>
            </b:if>
            
            <!-- Post Content -->
            <div class='post-content'>
              <!-- Post Category/Labels -->
              <b:if cond='data:post.labels'>
                <div class='post-labels'>
                  <b:loop values='data:post.labels limit 2' var='label'>
                    <a class='post-label' expr:href='data:label.url' rel='tag'>
                      <data:label.name/>
                    </a>
                  </b:loop>
                </div>
              </b:if>
              
              <!-- Post Title -->
              <h3 class='post-title'>
                <a expr:href='data:post.url' expr:title='data:post.title'>
                  <data:post.title/>
                </a>
              </h3>
              
              <!-- Post Snippet -->
              <b:if cond='data:post.snippet'>
                <p class='post-snippet'>
                  <data:post.snippet/>
                </p>
              </b:if>
              
              <!-- Post Footer -->
              <div class='post-footer'>
                <a class='read-more-btn' expr:href='data:post.url'>
                  <span>Read Case Study</span>
                  <svg width='20' height='20' viewBox='0 0 20 20' fill='none'>
                    <path d='M7.5 15L12.5 10L7.5 5' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
                  </svg>
                </a>
                
                <!-- Reading Time Estimate (Optional) -->
                <span class='post-meta'>
                  <svg width='16' height='16' viewBox='0 0 16 16' fill='none'>
                    <circle cx='8' cy='8' r='6' stroke='currentColor' stroke-width='1.5'/>
                    <path d='M8 4V8L11 11' stroke='currentColor' stroke-width='1.5' stroke-linecap='round'/>
                  </svg>
                  <span>5 min read</span>
                </span>
              </div>
            </div>
          </article>
        </b:includable>
        
        <!-- Pagination -->
        <b:includable id='postPagination'>
          <b:if cond='data:posts.size != 0'>
            <nav class='blog-pagination'>
              <b:include name='previousPageLink'/>
              <b:include name='nextPageLink'/>
            </nav>
          </b:if>
        </b:includable>
        
        <!-- Previous Page Link -->
        <b:includable id='previousPageLink'>
          <b:if cond='data:olderPageUrl'>
            <a class='pagination-btn pagination-prev' expr:href='data:olderPageUrl' expr:title='data:messages.olderPosts'>
              <svg width='20' height='20' viewBox='0 0 20 20' fill='none'>
                <path d='M12.5 15L7.5 10L12.5 5' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
              </svg>
              <span>Previous</span>
            </a>
          </b:if>
        </b:includable>
        
        <!-- Next Page Link -->
        <b:includable id='nextPageLink'>
          <b:if cond='data:newerPageUrl'>
            <a class='pagination-btn pagination-next' expr:href='data:newerPageUrl' expr:title='data:messages.newerPosts'>
              <span>Next</span>
              <svg width='20' height='20' viewBox='0 0 20 20' fill='none'>
                <path d='M7.5 15L12.5 10L7.5 5' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
              </svg>
            </a>
          </b:if>
        </b:includable>
        
        <!-- Hide default elements -->
        <b:includable id='post-share-buttons'/>
        <b:includable id='postFooter'/>
        <b:includable id='postAuthor'/>
        <b:includable id='postTimestamp'/>
        <b:includable id='feedLinks'/>
      </b:widget>
    </b:section>
    
    <!-- Projects/Ventures Section -->
    <b:section class='projects-section section bg-secondary' id='projects' maxwidgets='6' name='Projects/Ventures' showaddelement='yes'>
      <!-- Projects Grid Widget -->
      <b:widget id='HTML2' locked='false' title='Featured Projects' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<div class="projects-container">
  <div class="container">
    <!-- Section Header -->
    <div class="section-header">
      <span class="section-label">Portfolio</span>
      <h2 class="section-title">Featured Projects & Ventures</h2>
      <p class="section-description">
        Explore some of my key initiatives and products that are making an impact
      </p>
    </div>
    
    <!-- Projects Grid -->
    <div class="projects-grid">
      
      <!-- Project Card 1: The Chattala -->
      <div class="project-card" data-project="1">
        <div class="project-card-inner">
          <div class="project-icon">
            <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
              <rect width="40" height="40" rx="8" fill="url(#gradient1)"/>
              <path d="M20 12L28 16V24C28 28 20 32 20 32C20 32 12 28 12 24V16L20 12Z" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <defs>
                <linearGradient id="gradient1" x1="0" y1="0" x2="40" y2="40">
                  <stop offset="0%" stop-color="#2563eb"/>
                  <stop offset="100%" stop-color="#7c3aed"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          
          <div class="project-badge">Platform</div>
          
          <h3 class="project-title" data-editable="title-1">The Chattala</h3>
          
          <p class="project-description" data-editable="desc-1">
            A hyperlocal community platform connecting neighbors and local businesses. 
            Features real-time updates, marketplace, and community events management.
          </p>
          
          <div class="project-tech">
            <span class="tech-tag" data-editable="tech-1-1">Next.js</span>
            <span class="tech-tag" data-editable="tech-1-2">Prisma</span>
            <span class="tech-tag" data-editable="tech-1-3">Tailwind CSS</span>
            <span class="tech-tag" data-editable="tech-1-4">PostgreSQL</span>
          </div>
          
          <div class="project-footer">
            <a href="https://thechattala.com" class="project-link" data-editable="link-1" target="_blank" rel="noopener">
              <span>View Project</span>
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
            <div class="project-status">
              <span class="status-dot status-live"></span>
              <span class="status-text">Live</span>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Project Card 2: Intactic Systems -->
      <div class="project-card" data-project="2">
        <div class="project-card-inner">
          <div class="project-icon">
            <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
              <rect width="40" height="40" rx="8" fill="url(#gradient2)"/>
              <path d="M20 12V28M14 18L20 12L26 18M26 22L20 28L14 22" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <defs>
                <linearGradient id="gradient2" x1="0" y1="0" x2="40" y2="40">
                  <stop offset="0%" stop-color="#10b981"/>
                  <stop offset="100%" stop-color="#2563eb"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          
          <div class="project-badge">Agency</div>
          
          <h3 class="project-title" data-editable="title-2">Intactic Systems</h3>
          
          <p class="project-description" data-editable="desc-2">
            Full-service IT consulting and digital transformation agency. 
            Delivering custom software solutions, cloud infrastructure, and technical strategy.
          </p>
          
          <div class="project-tech">
            <span class="tech-tag" data-editable="tech-2-1">React</span>
            <span class="tech-tag" data-editable="tech-2-2">Node.js</span>
            <span class="tech-tag" data-editable="tech-2-3">AWS</span>
            <span class="tech-tag" data-editable="tech-2-4">Docker</span>
          </div>
          
          <div class="project-footer">
            <a href="https://intacticsystems.com" class="project-link" data-editable="link-2" target="_blank" rel="noopener">
              <span>View Project</span>
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
            <div class="project-status">
              <span class="status-dot status-live"></span>
              <span class="status-text">Live</span>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Project Card 3: Personal Portfolio -->
      <div class="project-card" data-project="3">
        <div class="project-card-inner">
          <div class="project-icon">
            <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
              <rect width="40" height="40" rx="8" fill="url(#gradient3)"/>
              <path d="M12 20H28M20 12L28 20L20 28" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <defs>
                <linearGradient id="gradient3" x1="0" y1="0" x2="40" y2="40">
                  <stop offset="0%" stop-color="#7c3aed"/>
                  <stop offset="100%" stop-color="#10b981"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          
          <div class="project-badge">Portfolio</div>
          
          <h3 class="project-title" data-editable="title-3">Personal Brand</h3>
          
          <p class="project-description" data-editable="desc-3">
            My digital home showcasing work, thoughts, and expertise. 
            Built with modern web technologies and optimized for performance and SEO.
          </p>
          
          <div class="project-tech">
            <span class="tech-tag" data-editable="tech-3-1">Blogger</span>
            <span class="tech-tag" data-editable="tech-3-2">Custom XML</span>
            <span class="tech-tag" data-editable="tech-3-3">JavaScript</span>
            <span class="tech-tag" data-editable="tech-3-4">CSS3</span>
          </div>
          
          <div class="project-footer">
            <a href="https://yourportfolio.com" class="project-link" data-editable="link-3" target="_blank" rel="noopener">
              <span>View Project</span>
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
            <div class="project-status">
              <span class="status-dot status-live"></span>
              <span class="status-text">Live</span>
            </div>
          </div>
        </div>
      </div>
      
    </div>
    
    <!-- View All Projects Link -->
    <div class="projects-cta">
      <a href="#all-projects" class="link-with-arrow" data-editable="view-all">
        <span>View All Projects</span>
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>
  </div>
</div>

<style>
/* Projects Section Styles */
.projects-container {
  padding: var(--spacing-3xl) 0;
}

/* Section Header */
.section-header {
  text-align: center;
  margin-bottom: var(--spacing-3xl);
  animation: fadeInUp 0.8s ease-out;
}

.section-label {
  display: inline-block;
  font-size: var(--text-sm);
  font-weight: 600;
  color: var(--primary-color);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  margin-bottom: var(--spacing-md);
}

.section-title {
  font-size: var(--text-4xl);
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: var(--spacing-md);
}

.section-description {
  font-size: var(--text-lg);
  color: var(--text-secondary);
  max-width: 600px;
  margin: 0 auto;
}

/* Projects Grid */
.projects-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: var(--spacing-xl);
  margin-bottom: var(--spacing-2xl);
}

/* Project Card */
.project-card {
  position: relative;
  background: var(--bg-primary);
  border-radius: var(--border-radius-lg);
  overflow: hidden;
  transition: var(--transition);
  animation: fadeInUp 0.8s ease-out backwards;
}

.project-card[data-project="1"] {
  animation-delay: 0.1s;
}

.project-card[data-project="2"] {
  animation-delay: 0.2s;
}

.project-card[data-project="3"] {
  animation-delay: 0.3s;
}

.project-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.3s ease;
}

.project-card:hover::before {
  transform: scaleX(1);
}

.project-card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-xl);
}

.project-card-inner {
  padding: var(--spacing-xl);
  display: flex;
  flex-direction: column;
  height: 100%;
}

/* Project Icon */
.project-icon {
  margin-bottom: var(--spacing-lg);
}

/* Project Badge */
.project-badge {
  display: inline-block;
  align-self: flex-start;
  padding: var(--spacing-xs) var(--spacing-md);
  background: var(--bg-secondary);
  color: var(--text-secondary);
  font-size: var(--text-xs);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  border-radius: 4px;
  margin-bottom: var(--spacing-md);
}

/* Project Title */
.project-title {
  font-size: var(--text-2xl);
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: var(--spacing-md);
  line-height: 1.3;
}

/* Project Description */
.project-description {
  font-size: var(--text-base);
  color: var(--text-secondary);
  line-height: 1.7;
  margin-bottom: var(--spacing-lg);
  flex-grow: 1;
}

/* Tech Stack Tags */
.project-tech {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-xs);
  margin-bottom: var(--spacing-lg);
}

.tech-tag {
  display: inline-block;
  padding: var(--spacing-xs) var(--spacing-md);
  background: linear-gradient(135deg, rgba(37, 99, 235, 0.1), rgba(124, 58, 237, 0.1));
  color: var(--primary-color);
  font-size: var(--text-xs);
  font-weight: 600;
  border-radius: var(--border-radius);
  border: 1px solid rgba(37, 99, 235, 0.2);
  transition: var(--transition);
}

.tech-tag:hover {
  background: var(--primary-color);
  color: white;
  border-color: var(--primary-color);
}

/* Project Footer */
.project-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: var(--spacing-md);
  border-top: 1px solid var(--border-color);
}

.project-link {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  font-size: var(--text-base);
  font-weight: 600;
  color: var(--primary-color);
  text-decoration: none;
  transition: var(--transition);
}

.project-link:hover {
  gap: var(--spacing-sm);
  color: var(--secondary-color);
}

.project-link svg {
  transition: transform 0.3s ease;
}

.project-link:hover svg {
  transform: translateX(4px);
}

/* Project Status */
.project-status {
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
}

.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  animation: pulse-dot 2s ease-in-out infinite;
}

.status-dot.status-live {
  background: var(--accent-color);
}

.status-dot.status-development {
  background: #f59e0b;
}

.status-text {
  font-size: var(--text-xs);
  color: var(--text-secondary);
  font-weight: 500;
}

/* Projects CTA */
.projects-cta {
  text-align: center;
  margin-top: var(--spacing-2xl);
}

.link-with-arrow {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-sm);
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--primary-color);
  text-decoration: none;
  transition: var(--transition);
}

.link-with-arrow:hover {
  color: var(--secondary-color);
  gap: var(--spacing-md);
}

.link-with-arrow svg {
  transition: transform 0.3s ease;
}

.link-with-arrow:hover svg {
  transform: translateX(4px);
}

/* Responsive */
@media (max-width: 768px) {
  .section-title {
    font-size: var(--text-3xl);
  }
  
  .projects-grid {
    grid-template-columns: 1fr;
    gap: var(--spacing-lg);
  }
  
  .project-card-inner {
    padding: var(--spacing-lg);
  }
  
  .project-title {
    font-size: var(--text-xl);
  }
  
  .project-description {
    font-size: var(--text-sm);
  }
}

@media (max-width: 480px) {
  .projects-container {
    padding: var(--spacing-2xl) 0;
  }
  
  .section-header {
    margin-bottom: var(--spacing-2xl);
  }
  
  .section-title {
    font-size: var(--text-2xl);
  }
  
  .section-description {
    font-size: var(--text-base);
  }
}
</style>

<script>
// Optional: Add project card interaction enhancements
document.addEventListener('DOMContentLoaded', function() {
  const projectCards = document.querySelectorAll('.project-card');
  
  // Add click tracking or analytics here if needed
  projectCards.forEach(card => {
    card.addEventListener('mouseenter', function() {
      this.style.borderColor = 'var(--primary-color)';
    });
    
    card.addEventListener('mouseleave', function() {
      this.style.borderColor = '';
    });
  });
});
</script>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- Footer Section -->
    <b:section class='footer-section section bg-dark' id='footer' maxwidgets='4' name='Footer' showaddelement='yes'>
      <!-- Add Footer widgets from Layout tab -->
    </b:section>
  </body>
</html>
