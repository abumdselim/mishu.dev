<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:css='false' b:defaultwidgetversion='2' b:layoutsVersion='3' b:responsive='true' b:templateUrl='indie' b:templateVersion='2.0.0' expr:dir='data:blog.languageDirection' expr:lang='data:blog.locale'>
  <head>
    <meta charset='UTF-8'/>
    <meta content='width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=5.0, user-scalable=yes' name='viewport'/>
    <meta content='IE=edge' http-equiv='X-UA-Compatible'/>
    
    <!-- SEO Meta Tags -->
    <title><data:blog.pageTitle/></title>
    <b:if cond='data:blog.metaDescription'>
      <meta expr:content='data:blog.metaDescription' name='description'/>
    <b:else/>
      <meta content='Elite developer portfolio by Abu Md Selim (Mishu) - Full-stack developer, AI integration specialist, and founder of Intactic Systems. Crafting $10k+ digital experiences with cutting-edge technology.' name='description'/>
    </b:if>
    
    <meta content='portfolio, full-stack developer, AI integration, web development, Intactic Systems, The Chattala, freelance developer, Chittagong developer, React, Next.js, Node.js, tailwind' name='keywords'/>
    <meta content='index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1' name='robots'/>
    <meta content='Abu Md Selim (Mishu)' name='author'/>
    <meta content='7 days' name='revisit-after'/>
    
    <!-- Open Graph Meta Tags -->
    <meta expr:content='data:blog.pageTitle' property='og:title'/>
    <b:if cond='data:blog.metaDescription'>
      <meta expr:content='data:blog.metaDescription' property='og:description'/>
    <b:else/>
      <meta content='Elite developer portfolio by Abu Md Selim (Mishu) - Full-stack developer, AI integration specialist, and founder of Intactic Systems. Crafting $10k+ digital experiences with cutting-edge technology.' property='og:description'/>
    </b:if>
    <b:if cond='data:blog.postImageUrl'>
      <meta expr:content='data:blog.postImageUrl' property='og:image'/>
      <meta content='1200' property='og:image:width'/>
      <meta content='630' property='og:image:height'/>
    <b:else/>
      <meta content='https://via.placeholder.com/1200x630/0a0a0f/3b82f6?text=mishu.dev' property='og:image'/>
      <meta content='1200' property='og:image:width'/>
      <meta content='630' property='og:image:height'/>
    </b:if>
    <meta content='website' property='og:type'/>
    <meta expr:content='data:blog.url' property='og:url'/>
    <meta content='mishu.dev - Elite Developer Portfolio' property='og:site_name'/>
    <meta content='en_US' property='og:locale'/>
    
    <!-- Twitter Card Meta Tags -->
    <meta content='summary_large_image' name='twitter:card'/>
    <meta expr:content='data:blog.pageTitle' name='twitter:title'/>
    <b:if cond='data:blog.metaDescription'>
      <meta expr:content='data:blog.metaDescription' name='twitter:description'/>
    <b:else/>
      <meta content='Elite developer portfolio by Abu Md Selim (Mishu) - Full-stack developer, AI integration specialist, and founder of Intactic Systems.' name='twitter:description'/>
    </b:if>
    <b:if cond='data:blog.postImageUrl'>
      <meta expr:content='data:blog.postImageUrl' name='twitter:image'/>
    <b:else/>
      <meta content='https://via.placeholder.com/1200x630/0a0a0f/3b82f6?text=mishu.dev' name='twitter:image'/>
    </b:if>
    <meta content='@mishu_dev' name='twitter:site'/>
    <meta content='@mishu_dev' name='twitter:creator'/>
    
    <!-- Additional Meta Tags -->
    <meta content='#3b82f6' name='theme-color'/>
    <meta content='#3b82f6' name='msapplication-TileColor'/>
    <meta content='mishu.dev' name='apple-mobile-web-app-title'/>
    <meta content='yes' name='apple-mobile-web-app-capable'/>
    <meta content='black-translucent' name='apple-mobile-web-app-status-bar-style'/>
    
    <!-- Canonical URL -->
    <link expr:href='data:blog.url' rel='canonical'/>
    
    <!-- Google Fonts - Inter &amp; JetBrains Mono -->
    <link href='https://fonts.googleapis.com' rel='preconnect'/>
    <link crossorigin='anonymous' href='https://fonts.gstatic.com' rel='preconnect'/>
    <link href='https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&amp;family=JetBrains+Mono:wght@400;500;600;700&amp;display=swap' rel='stylesheet'/>
    
    <!-- Prism.js Syntax Highlighting -->
    <link href='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/themes/prism-tomorrow.min.css' rel='stylesheet'/>
    <link href='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/line-numbers/prism-line-numbers.min.css' rel='stylesheet'/>
    <link href='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/toolbar/prism-toolbar.min.css' rel='stylesheet'/>
    
    <!-- URL Cleanup Script - Remove ?m=1 parameter -->
    <script>
    //<![CDATA[
    (function() {
      // Remove ?m=1 parameter without breaking history
      if (window.location.search.indexOf('m=1') > -1) {
        const cleanUrl = window.location.href.replace(/[?&]m=1(&|$)/, '$1').replace(/[?&]$/, '');
        if (cleanUrl !== window.location.href) {
          window.history.replaceState({}, document.title, cleanUrl);
        }
      }
    })();
    //]]>
    </script>
    
    <!-- Production-Ready Enhancements -->
    <style>
    /*<![CDATA[*/
    /* Smooth Page Reveal Animation */
    body {
      animation: smoothReveal 0.6s ease-out;
    }
    
    @keyframes smoothReveal {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    
    /* Custom Scrollbar - Dark Glassmorphic Theme */
    ::-webkit-scrollbar {
      width: 12px;
      height: 12px;
    }
    
    ::-webkit-scrollbar-track {
      background: #0a0a0f;
    }
    
    ::-webkit-scrollbar-thumb {
      background: linear-gradient(180deg, #3b82f6 0%, #8b5cf6 100%);
      border-radius: 6px;
      border: 2px solid #0a0a0f;
    }
    
    ::-webkit-scrollbar-thumb:hover {
      background: linear-gradient(180deg, #2563eb 0%, #7c3aed 100%);
    }
    
    /* Custom Text Selection */
    ::selection {
      background: rgba(59, 130, 246, 0.3);
      color: #ffffff;
    }
    
    ::-moz-selection {
      background: rgba(59, 130, 246, 0.3);
      color: #ffffff;
    }
    
    /* Enhanced Focus States for Accessibility */
    *:focus-visible {
      outline: 2px solid #3b82f6;
      outline-offset: 2px;
      border-radius: 4px;
    }
    
    a:focus-visible,
    button:focus-visible {
      outline: 2px solid #3b82f6;
      outline-offset: 4px;
      box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.2);
    }
    /*]]>*/
    </style>
    
    <b:skin version='2.0.0'><![CDATA[
    /*
    =====================================================
    Theme Name: Modern Tech Portfolio (Dark Edition)
    Description: Premium dark-mode portfolio with glassmorphism
    Version: 2.0.0
    Author: Expert Blogger Theme Developer
    =====================================================
    */
    
    /* CSS Variables - Modern Dark Tech Portfolio */
    :root {
      /* Dark Mode Colors */
      --bg-primary: #0a0a0f;
      --bg-secondary: #13131a;
      --bg-tertiary: #1a1a24;
      --bg-elevated: #1f1f2e;
      
      /* Neon Accents */
      --primary-color: #3b82f6;
      --primary-glow: rgba(59, 130, 246, 0.5);
      --secondary-color: #8b5cf6;
      --secondary-glow: rgba(139, 92, 246, 0.5);
      --accent-color: #06b6d4;
      --accent-glow: rgba(6, 182, 212, 0.5);
      --success-color: #10b981;
      
      /* Text Colors */
      --text-primary: #f9fafb;
      --text-secondary: #9ca3af;
      --text-tertiary: #6b7280;
      --text-muted: #4b5563;
      
      /* Border & Divider */
      --border-color: rgba(255, 255, 255, 0.1);
      --border-light: rgba(255, 255, 255, 0.05);
      --divider-color: rgba(255, 255, 255, 0.08);
      
      /* Glassmorphism */
      --glass-bg: rgba(31, 31, 46, 0.7);
      --glass-border: rgba(255, 255, 255, 0.1);
      --glass-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
      
      /* Shadows */
      --shadow-sm: 0 1px 3px 0 rgba(0, 0, 0, 0.5);
      --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.5);
      --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.5);
      --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.5);
      --shadow-2xl: 0 25px 50px -12px rgba(0, 0, 0, 0.6);
      --shadow-glow: 0 0 20px var(--primary-glow);
      --shadow-glow-secondary: 0 0 20px var(--secondary-glow);
      
      /* Typography */
      --font-primary: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      --font-mono: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;
      
      /* Font Sizes */
      --text-xs: 0.75rem;
      --text-sm: 0.875rem;
      --text-base: 1rem;
      --text-lg: 1.125rem;
      --text-xl: 1.25rem;
      --text-2xl: 1.5rem;
      --text-3xl: 1.875rem;
      --text-4xl: 2.25rem;
      --text-5xl: 3rem;
      --text-6xl: 3.75rem;
      
      /* Spacing */
      --spacing-xs: 0.5rem;
      --spacing-sm: 0.75rem;
      --spacing-md: 1rem;
      --spacing-lg: 1.5rem;
      --spacing-xl: 2rem;
      --spacing-2xl: 3rem;
      --spacing-3xl: 4rem;
      --spacing-4xl: 5rem;
      
      /* Layout */
      --max-width: 1280px;
      --border-radius: 0.5rem;
      --border-radius-lg: 1rem;
      --border-radius-xl: 1.5rem;
      --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      --transition-fast: all 0.15s cubic-bezier(0.4, 0, 0.2, 1);
      
      /* Z-Index Layers */
      --z-base: 1;
      --z-elevated: 10;
      --z-header: 100;
      --z-modal: 1000;
    }
    
    /* Global Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    *::before,
    *::after {
      box-sizing: border-box;
    }
    
    /* Base Styles */
    html {
      font-size: 16px;
      scroll-behavior: smooth;
      -webkit-text-size-adjust: 100%;
    }
    
    body {
      font-family: var(--font-primary);
      color: var(--text-primary);
      background-color: var(--bg-primary);
      background-image: 
        radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.05) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(139, 92, 246, 0.05) 0%, transparent 50%),
        radial-gradient(circle at 40% 20%, rgba(6, 182, 212, 0.03) 0%, transparent 50%);
      background-attachment: fixed;
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      overflow-x: hidden;
    }
    
    /* Container */
    .container {
      max-width: var(--max-width);
      margin: 0 auto;
      padding: 0 var(--spacing-lg);
      width: 100%;
    }
    
    /* Section Spacing */
    .section {
      padding: var(--spacing-4xl) 0;
      position: relative;
    }
    
    /* Typography */
    h1, h2, h3, h4, h5, h6 {
      font-weight: 700;
      line-height: 1.2;
      color: var(--text-primary);
      letter-spacing: -0.02em;
    }
    
    h1 {
      font-size: clamp(2.5rem, 5vw, var(--text-6xl));
      font-weight: 800;
    }
    
    h2 {
      font-size: clamp(2rem, 4vw, var(--text-5xl));
      font-weight: 700;
    }
    
    h3 {
      font-size: clamp(1.5rem, 3vw, var(--text-3xl));
    }
    
    p {
      line-height: 1.7;
    }
    
    a {
      color: var(--primary-color);
      text-decoration: none;
      transition: var(--transition);
    }
    
    a:hover {
      color: var(--secondary-color);
    }
    
    img {
      max-width: 100%;
      height: auto;
      display: block;
    }
    
    /* Utility Classes */
    .text-center {
      text-align: center;
    }
    
    .bg-secondary {
      background-color: var(--bg-secondary);
    }
    
    .bg-tertiary {
      background-color: var(--bg-tertiary);
    }
    
    .bg-dark {
      background-color: var(--bg-primary);
    }
    
    /* Glassmorphism Effects */
    .glass {
      background: var(--glass-bg);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border: 1px solid var(--glass-border);
    }
    
    /* PREMIUM FLOATING HEADER */
    .header-section {
      position: fixed !important;
      top: 16px;
      left: 50%;
      transform: translateX(-50%);
      width: calc(100% - 32px);
      max-width: 1200px;
      z-index: var(--z-header) !important;
      padding: 0 !important;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      animation: floatIn 0.8s ease-out;
    }
    
    @keyframes floatIn {
      from {
        opacity: 0;
        transform: translate(-50%, -20px);
      }
      to {
        opacity: 1;
        transform: translate(-50%, 0);
      }
    }
    
    .header-section.scrolled {
      top: 8px;
      width: calc(100% - 64px);
    }
    
    .floating-nav {
      background: rgba(13, 13, 26, 0.85);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid rgba(59, 130, 246, 0.2);
      border-radius: 16px;
      padding: 12px 24px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4),
                  0 0 0 1px rgba(59, 130, 246, 0.1),
                  inset 0 1px 0 rgba(255, 255, 255, 0.1);
      display: flex;
      align-items: center;
      justify-content: space-between;
      position: relative;
      overflow: hidden;
    }
    
    .floating-nav::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 200%;
      height: 100%;
      background: linear-gradient(90deg, 
        transparent, 
        rgba(59, 130, 246, 0.05), 
        transparent);
      animation: shimmer 3s infinite;
    }
    
    @keyframes shimmer {
      0% { left: -100%; }
      100% { left: 100%; }
    }
    
    .nav-brand {
      display: flex;
      align-items: center;
      gap: 12px;
      font-weight: 700;
      font-size: 1.25rem;
      color: var(--text-primary);
      font-family: var(--font-mono);
      position: relative;
      z-index: 1;
    }
    
    .nav-brand-logo {
      width: 36px;
      height: 36px;
      background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 800;
      font-size: 1.125rem;
      box-shadow: 0 4px 12px var(--primary-glow);
      transition: var(--transition);
    }
    
    .nav-brand:hover .nav-brand-logo {
      transform: rotate(5deg) scale(1.05);
      box-shadow: 0 6px 20px var(--primary-glow);
    }
    
    .nav-links {
      display: flex;
      align-items: center;
      gap: 8px;
      list-style: none;
      position: relative;
      z-index: 1;
    }
    
    .nav-link {
      padding: 8px 16px;
      font-size: 0.9375rem;
      font-weight: 500;
      color: var(--text-secondary);
      border-radius: 8px;
      transition: var(--transition);
      position: relative;
      overflow: hidden;
      font-family: var(--font-mono);
    }
    
    .nav-link::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      border-radius: 50%;
      background: rgba(59, 130, 246, 0.15);
      transform: translate(-50%, -50%);
      transition: width 0.4s, height 0.4s;
    }
    
    .nav-link:hover {
      color: var(--primary-color);
      background: rgba(59, 130, 246, 0.1);
    }
    
    .nav-link:hover::before {
      width: 200px;
      height: 200px;
    }
    
    .nav-link.active {
      color: var(--text-primary);
      background: rgba(59, 130, 246, 0.15);
      box-shadow: inset 0 0 0 1px rgba(59, 130, 246, 0.3);
    }
    
    /* Header "Hire Me" CTA Button */
    .hire-me-btn {
      padding: 10px 24px;
      border-radius: 10px;
      background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
      border: 1px solid rgba(139, 92, 246, 0.3);
      color: #ffffff;
      font-size: 14px;
      font-weight: 600;
      text-decoration: none;
      position: relative;
      overflow: hidden;
      transition: var(--transition);
      box-shadow: 0 4px 15px rgba(139, 92, 246, 0.3);
      z-index: 1;
    }
    
    .hire-me-btn::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      background: radial-gradient(circle, rgba(255, 255, 255, 0.2) 0%, transparent 70%);
      border-radius: 50%;
      transform: translate(-50%, -50%);
      transition: width 0.6s ease, height 0.6s ease;
      z-index: -1;
    }
    
    .hire-me-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 30px rgba(139, 92, 246, 0.5);
      border-color: rgba(139, 92, 246, 0.6);
    }
    
    .hire-me-btn:hover::before {
      width: 200px;
      height: 200px;
    }
    
    .hire-me-btn:active {
      transform: translateY(0);
    }
    
    .theme-toggle {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.2);
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      transition: var(--transition);
      position: relative;
      z-index: 1;
    }
    
    .theme-toggle:hover {
      background: rgba(59, 130, 246, 0.2);
      transform: rotate(180deg);
      box-shadow: 0 0 20px var(--primary-glow);
    }
    
    .theme-toggle svg {
      width: 20px;
      height: 20px;
      color: var(--primary-color);
    }
    
    .mobile-menu-toggle {
      display: none;
      width: 40px;
      height: 40px;
      border-radius: 10px;
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.2);
      align-items: center;
      justify-content: center;
      cursor: pointer;
      transition: var(--transition);
      position: relative;
      z-index: 1;
    }
    
    .mobile-menu-toggle:hover {
      background: rgba(59, 130, 246, 0.2);
    }
    
    /* Mobile Bottom Navigation */
    .mobile-bottom-nav {
      display: none;
      position: fixed;
      bottom: 16px;
      left: 50%;
      transform: translateX(-50%);
      width: calc(100% - 32px);
      max-width: 400px;
      background: rgba(13, 13, 26, 0.95);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid rgba(59, 130, 246, 0.3);
      border-radius: 24px;
      padding: 12px 16px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.6),
                  0 0 0 1px rgba(59, 130, 246, 0.2);
      z-index: calc(var(--z-header) + 1);
      animation: slideUp 0.5s ease-out;
    }
    
    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translate(-50%, 20px);
      }
      to {
        opacity: 1;
        transform: translate(-50%, 0);
      }
    }
    
    .mobile-nav-items {
      display: flex;
      justify-content: space-around;
      align-items: center;
      gap: 8px;
    }
    
    .mobile-nav-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 4px;
      padding: 8px 12px;
      border-radius: 12px;
      color: var(--text-secondary);
      font-size: 0.6875rem;
      font-weight: 500;
      font-family: var(--font-mono);
      transition: var(--transition);
      cursor: pointer;
      flex: 1;
      text-align: center;
    }
    
    .mobile-nav-item svg {
      width: 20px;
      height: 20px;
    }
    
    .mobile-nav-item.active,
    .mobile-nav-item:hover {
      background: rgba(59, 130, 246, 0.15);
      color: var(--primary-color);
    }
    
    /* BENTO GRID ARCHITECTURE */
    .bento-grid {
      display: grid;
      grid-template-columns: repeat(12, 1fr);
      gap: var(--spacing-lg);
      margin-bottom: var(--spacing-3xl);
    }
    
    .bento-item {
      background: var(--glass-bg);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border: 1px solid rgba(59, 130, 246, 0.15);
      border-radius: 20px;
      padding: var(--spacing-xl);
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }
    
    .bento-item::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, 
        transparent,
        var(--primary-color),
        var(--secondary-color),
        transparent);
      transform: translateX(-100%);
      transition: transform 0.6s ease;
    }
    
    .bento-item:hover::before {
      transform: translateX(100%);
    }
    
    .bento-item:hover {
      transform: translateY(-8px);
      border-color: rgba(59, 130, 246, 0.4);
      box-shadow: 0 20px 60px rgba(59, 130, 246, 0.3),
                  0 0 0 1px rgba(59, 130, 246, 0.2);
    }
    
    /* Bento Grid Layouts */
    .bento-large {
      grid-column: span 8;
    }
    
    .bento-medium {
      grid-column: span 6;
    }
    
    .bento-small {
      grid-column: span 4;
    }
    
    .bento-tall {
      grid-row: span 2;
    }
    
    /* MAGNETIC HOVER EFFECTS */
    .magnetic-card {
      transition: transform 0.1s ease;
      transform-style: preserve-3d;
    }
    
    .magnetic-card:hover {
      cursor: pointer;
    }
    
    /* Premium CTA Button with Magnetic Effect */
    .magnetic-btn {
      position: relative;
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-sm);
      padding: 16px 32px;
      font-size: var(--text-base);
      font-weight: 600;
      font-family: var(--font-mono);
      color: white;
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
      border: none;
      border-radius: 12px;
      cursor: pointer;
      overflow: hidden;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 4px 15px var(--primary-glow),
                  0 0 0 1px rgba(255, 255, 255, 0.1);
    }
    
    .magnetic-btn::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.2);
      transform: translate(-50%, -50%);
      transition: width 0.6s, height 0.6s;
    }
    
    .magnetic-btn:hover::before {
      width: 300px;
      height: 300px;
    }
    
    .magnetic-btn:hover {
      transform: translateY(-4px) scale(1.02);
      box-shadow: 0 12px 30px var(--primary-glow),
                  0 0 0 1px rgba(255, 255, 255, 0.2),
                  0 0 30px var(--primary-glow);
    }
    
    .magnetic-btn:active {
      transform: translateY(-2px) scale(0.98);
    }
    
    /* ANIMATED GRADIENT ORB */
    .gradient-orb {
      position: absolute;
      width: 600px;
      height: 600px;
      border-radius: 50%;
      filter: blur(120px);
      opacity: 0.3;
      animation: float 20s ease-in-out infinite;
      pointer-events: none;
      z-index: 0;
    }
    
    .gradient-orb-1 {
      background: radial-gradient(circle, var(--primary-color) 0%, transparent 70%);
      top: -200px;
      left: -200px;
      animation-delay: 0s;
    }
    
    .gradient-orb-2 {
      background: radial-gradient(circle, var(--secondary-color) 0%, transparent 70%);
      bottom: -200px;
      right: -200px;
      animation-delay: 10s;
      animation-direction: reverse;
    }
    
    .gradient-orb-3 {
      background: radial-gradient(circle, var(--accent-color) 0%, transparent 70%);
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      animation-delay: 5s;
      width: 400px;
      height: 400px;
    }
    
    @keyframes float {
      0%, 100% {
        transform: translate(0, 0) scale(1);
      }
      25% {
        transform: translate(30px, -30px) scale(1.1);
      }
      50% {
        transform: translate(-20px, 20px) scale(0.9);
      }
      75% {
        transform: translate(20px, 30px) scale(1.05);
      }
    }
    
    /* PREMIUM SCROLL ANIMATIONS */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(40px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
    
    .scroll-reveal {
      opacity: 0;
      animation: fadeInUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards;
    }
    
    .scroll-reveal-1 { animation-delay: 0.1s; }
    .scroll-reveal-2 { animation-delay: 0.2s; }
    .scroll-reveal-3 { animation-delay: 0.3s; }
    .scroll-reveal-4 { animation-delay: 0.4s; }
    .scroll-reveal-5 { animation-delay: 0.5s; }
    
    /* ELITE TYPOGRAPHY */
    .headline-premium {
      font-size: clamp(2.5rem, 6vw, 4.5rem);
      font-weight: 800;
      line-height: 1.1;
      letter-spacing: -0.03em;
      background: linear-gradient(135deg, 
        var(--text-primary) 0%, 
        var(--primary-color) 50%, 
        var(--secondary-color) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: gradientShift 8s ease infinite;
      background-size: 200% 200%;
    }
    
    @keyframes gradientShift {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }
    
    .text-glow {
      text-shadow: 0 0 20px var(--primary-glow),
                   0 0 40px var(--primary-glow);
    }
    
    /* MICRO-INTERACTIONS */
    .hover-lift {
      transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .hover-lift:hover {
      transform: translateY(-4px);
    }
    
    .hover-glow {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .hover-glow:hover {
      box-shadow: 0 0 30px var(--primary-glow),
                  0 0 60px var(--primary-glow);
    }
    
    .pulse-glow {
      animation: pulseGlow 3s ease-in-out infinite;
    }
    
    @keyframes pulseGlow {
      0%, 100% {
        box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
      }
      50% {
        box-shadow: 0 0 40px rgba(59, 130, 246, 0.6);
      }
    }
      box-shadow: var(--glass-shadow);
    }
    
    .glass-card {
      background: rgba(26, 26, 36, 0.6);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: var(--border-radius-lg);
    }
    
    /* Section Header Styles */
    .section-header {
      text-align: center;
      margin-bottom: var(--spacing-3xl);
      animation: fadeInUp 0.8s ease-out;
    }
    
    .section-label {
      display: inline-block;
      font-family: var(--font-mono);
      font-size: var(--text-sm);
      font-weight: 600;
      color: var(--primary-color);
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: var(--spacing-md);
      padding: var(--spacing-xs) var(--spacing-md);
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.2);
      border-radius: var(--border-radius);
    }
    
    .section-title {
      font-size: clamp(2rem, 4vw, var(--text-4xl));
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: var(--spacing-md);
      background: linear-gradient(135deg, var(--text-primary) 0%, var(--text-secondary) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    .section-description {
      font-size: var(--text-lg);
      color: var(--text-secondary);
      max-width: 700px;
      margin: 0 auto;
      line-height: 1.7;
    }
    
    /* Widget Styles */
    .widget {
      margin-bottom: var(--spacing-lg);
    }
    
    .widget-title {
      font-size: var(--text-xl);
      font-weight: 600;
      margin-bottom: var(--spacing-md);
      color: var(--text-primary);
    }
    
    /* Animations */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
    
    @keyframes pulse {
      0%, 100% {
        opacity: 1;
      }
      50% {
        opacity: 0.7;
      }
    }
    
    @keyframes pulse-glow {
      0%, 100% {
        box-shadow: 0 0 10px var(--primary-glow);
      }
      50% {
        box-shadow: 0 0 20px var(--primary-glow);
      }
    }
    
    @keyframes shimmer {
      0% {
        background-position: -1000px 0;
      }
      100% {
        background-position: 1000px 0;
      }
    }
    
    /* Scrollbar Styling */
    ::-webkit-scrollbar {
      width: 10px;
      height: 10px;
    }
    
    ::-webkit-scrollbar-track {
      background: var(--bg-secondary);
    }
    
    ::-webkit-scrollbar-thumb {
      background: var(--bg-elevated);
      border-radius: 5px;
      border: 2px solid var(--bg-secondary);
    }
    
    ::-webkit-scrollbar-thumb:hover {
      background: rgba(59, 130, 246, 0.5);
    }
    
    /* Selection */
    ::selection {
      background: var(--primary-color);
      color: white;
    }
    
    ::-moz-selection {
      background: var(--primary-color);
      color: white;
    }
    
    /* Focus Styles */
    :focus-visible {
      outline: 2px solid var(--primary-color);
      outline-offset: 2px;
    }
    
    /* Responsive Base */
    @media (max-width: 1024px) {
      :root {
        --spacing-3xl: 3rem;
        --spacing-4xl: 4rem;
      }
      
      .section {
        padding: var(--spacing-3xl) 0;
      }
    }
    
    @media (max-width: 768px) {
      :root {
        --spacing-2xl: 2rem;
        --spacing-3xl: 2.5rem;
        --spacing-4xl: 3rem;
      }
      
      .container {
        padding: 0 var(--spacing-md);
      }
      
      .section {
        padding: var(--spacing-2xl) 0;
      }
      
      .section-header {
        margin-bottom: var(--spacing-2xl);
      }
    }
    
    @media (max-width: 480px) {
      html {
        font-size: 14px;
      }
      
      .container {
        padding: 0 var(--spacing-sm);
      }
    }
    
    /* Prism.js Code Block Styling */
    pre[class*="language-"] {
      margin: var(--spacing-lg) 0;
      padding: var(--spacing-lg);
      border-radius: var(--border-radius-lg);
      background: #1e1e1e !important;
      border: 1px solid var(--border-color);
      overflow-x: auto;
      font-family: var(--font-mono);
      font-size: 0.875rem;
      line-height: 1.6;
      box-shadow: var(--shadow-lg);
    }
    
    code[class*="language-"] {
      font-family: var(--font-mono);
      font-size: 0.875rem;
      text-shadow: none;
    }
    
    /* Inline code */
    :not(pre) > code[class*="language-"] {
      padding: 0.2em 0.4em;
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.2);
      border-radius: 4px;
      color: var(--primary-color);
      font-size: 0.85em;
    }
    
    :not(pre) > code {
      padding: 0.2em 0.4em;
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.2);
      border-radius: 4px;
      color: var(--primary-color);
      font-size: 0.85em;
      font-family: var(--font-mono);
    }
    
    /* Line numbers styling */
    .line-numbers .line-numbers-rows {
      border-right: 1px solid rgba(255, 255, 255, 0.1);
      background: rgba(0, 0, 0, 0.2);
    }
    
    .line-numbers-rows > span:before {
      color: rgba(255, 255, 255, 0.3);
    }
    
    /* Toolbar styling */
    div.code-toolbar > .toolbar {
      opacity: 0;
      transition: opacity 0.3s ease;
    }
    
    div.code-toolbar:hover > .toolbar {
      opacity: 1;
    }
    
    div.code-toolbar > .toolbar > .toolbar-item > button,
    div.code-toolbar > .toolbar > .toolbar-item > span {
      background: rgba(59, 130, 246, 0.2);
      color: var(--text-primary);
      border: 1px solid rgba(59, 130, 246, 0.3);
      padding: 0.3em 0.6em;
      border-radius: 4px;
      font-family: var(--font-mono);
      font-size: 0.75rem;
      transition: var(--transition);
    }
    
    div.code-toolbar > .toolbar > .toolbar-item > button:hover {
      background: var(--primary-color);
      color: white;
    }
    
    div.code-toolbar > .toolbar > .toolbar-item > button[data-copy-state="copy-success"] {
      background: var(--success-color);
      color: white;
    }
    
    /* Scrollbar for code blocks */
    pre[class*="language-"]::-webkit-scrollbar {
      height: 8px;
    }
    
    pre[class*="language-"]::-webkit-scrollbar-track {
      background: rgba(0, 0, 0, 0.3);
      border-radius: 4px;
    }
    
    pre[class*="language-"]::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.2);
      border-radius: 4px;
    }
    
    pre[class*="language-"]::-webkit-scrollbar-thumb:hover {
      background: rgba(255, 255, 255, 0.3);
    }
    
    /* Print Styles */
    @media print {
      body {
        background: white;
        color: black;
      }
      
      .glass,
      .glass-card {
        background: white;
        border: 1px solid #ccc;
      }
      
      pre[class*="language-"] {
        background: #f5f5f5 !important;
        border: 1px solid #ddd;
      }
    }
    
    ]]></b:skin>
    
    <b:template-skin><![CDATA[
    /* Additional Component Styles */
    
    /* Blog Section Styles (Case Studies) */
    .blog-container {
      padding: var(--spacing-4xl) 0;
    }
    
    .posts-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--spacing-xl);
      margin-bottom: var(--spacing-2xl);
    }
    
    .post-card {
      overflow: hidden;
      transition: var(--transition);
      display: flex;
      flex-direction: column;
      height: 100%;
    }
    
    .post-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 12px 40px rgba(59, 130, 246, 0.3);
      border-color: rgba(59, 130, 246, 0.4);
    }
    
    .post-thumbnail {
      position: relative;
      width: 100%;
      height: 240px;
      overflow: hidden;
      background: linear-gradient(135deg, var(--bg-tertiary) 0%, var(--bg-elevated) 100%);
    }
    
    .post-thumbnail img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.5s ease;
    }
    
    .post-card:hover .post-thumbnail img {
      transform: scale(1.1);
    }
    
    .post-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, rgba(59, 130, 246, 0.8), rgba(139, 92, 246, 0.8));
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
      transition: opacity 0.3s ease;
    }
    
    .post-card:hover .post-overlay {
      opacity: 1;
    }
    
    .overlay-icon {
      transform: scale(0.8);
      transition: transform 0.3s ease;
    }
    
    .post-card:hover .overlay-icon {
      transform: scale(1);
    }
    
    .post-content {
      padding: var(--spacing-xl);
      display: flex;
      flex-direction: column;
      flex-grow: 1;
    }
    
    .post-labels {
      display: flex;
      gap: var(--spacing-xs);
      margin-bottom: var(--spacing-md);
      flex-wrap: wrap;
    }
    
    .post-label {
      display: inline-block;
      padding: var(--spacing-xs) var(--spacing-sm);
      background: rgba(59, 130, 246, 0.1);
      color: var(--primary-color);
      font-size: var(--text-xs);
      font-weight: 600;
      font-family: var(--font-mono);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      border-radius: var(--border-radius);
      border: 1px solid rgba(59, 130, 246, 0.2);
      text-decoration: none;
      transition: var(--transition);
    }
    
    .post-label:hover {
      background: var(--primary-color);
      color: white;
      border-color: var(--primary-color);
    }
    
    .post-title {
      font-size: var(--text-xl);
      font-weight: 700;
      line-height: 1.3;
      margin-bottom: var(--spacing-md);
    }
    
    .post-title a {
      color: var(--text-primary);
      text-decoration: none;
      transition: var(--transition);
    }
    
    .post-title a:hover {
      color: var(--primary-color);
    }
    
    .post-snippet {
      font-size: var(--text-base);
      color: var(--text-secondary);
      line-height: 1.7;
      margin-bottom: var(--spacing-lg);
      flex-grow: 1;
      display: -webkit-box;
      -webkit-line-clamp: 3;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    
    .post-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-top: var(--spacing-md);
      border-top: 1px solid var(--border-color);
      gap: var(--spacing-md);
    }
    
    .read-more-btn {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-xs);
      padding: var(--spacing-sm) var(--spacing-lg);
      background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
      color: white;
      font-size: var(--text-sm);
      font-weight: 600;
      font-family: var(--font-mono);
      border-radius: var(--border-radius);
      text-decoration: none;
      transition: var(--transition);
    }
    
    .read-more-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px var(--primary-glow);
      color: white;
    }
    
    .read-more-btn svg {
      transition: transform 0.3s ease;
    }
    
    .read-more-btn:hover svg {
      transform: translateX(4px);
    }
    
    .post-meta {
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
      font-size: var(--text-xs);
      color: var(--text-secondary);
      font-family: var(--font-mono);
    }
    
    /* Reading Time Display */
    .post-reading-time {
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
      font-size: var(--text-sm);
      color: var(--text-secondary);
      font-family: var(--font-mono);
      margin-bottom: var(--spacing-md);
      padding: var(--spacing-xs) 0;
    }
    
    .post-reading-time svg {
      color: var(--primary-color);
      flex-shrink: 0;
    }
    
    .reading-time-text {
      font-weight: 500;
    }
    
    .blog-pagination {
      display: flex;
      justify-content: center;
      gap: var(--spacing-md);
      margin-top: var(--spacing-3xl);
    }
    
    .pagination-btn {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-sm);
      padding: var(--spacing-md) var(--spacing-xl);
      background: rgba(59, 130, 246, 0.1);
      color: var(--primary-color);
      font-size: var(--text-base);
      font-weight: 600;
      font-family: var(--font-mono);
      border: 2px solid rgba(59, 130, 246, 0.2);
      border-radius: var(--border-radius);
      text-decoration: none;
      transition: var(--transition);
    }
    
    .pagination-btn:hover {
      background: var(--primary-color);
      color: white;
      border-color: var(--primary-color);
      transform: translateY(-2px);
      box-shadow: 0 4px 15px var(--primary-glow);
    }
    
    /* Load More Button */
    .load-more-container {
      display: flex;
      justify-content: center;
      margin-top: var(--spacing-3xl);
      margin-bottom: var(--spacing-2xl);
    }
    
    .load-more-btn {
      position: relative;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: var(--spacing-sm);
      padding: var(--spacing-lg) var(--spacing-2xl);
      background: rgba(59, 130, 246, 0.1);
      color: var(--primary-color);
      font-size: var(--text-base);
      font-weight: 600;
      font-family: var(--font-mono);
      border: 2px solid rgba(59, 130, 246, 0.3);
      border-radius: var(--border-radius);
      cursor: pointer;
      transition: all 0.3s ease;
      min-width: 200px;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    
    .load-more-btn:hover:not(:disabled) {
      background: rgba(59, 130, 246, 0.2);
      border-color: var(--primary-color);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
    }
    
    .load-more-btn:active:not(:disabled) {
      transform: translateY(0);
    }
    
    .load-more-btn:disabled {
      opacity: 0.5;
      cursor: not-allowed;
      background: rgba(100, 100, 100, 0.1);
      border-color: rgba(100, 100, 100, 0.2);
      color: var(--text-tertiary);
    }
    
    .load-more-btn.loading {
      pointer-events: none;
    }
    
    .load-more-btn.loading .btn-text {
      opacity: 0.6;
    }
    
    .load-more-spinner {
      display: none;
      width: 20px;
      height: 20px;
      border: 2px solid rgba(59, 130, 246, 0.2);
      border-top-color: var(--primary-color);
      border-radius: 50%;
      animation: spin 0.8s linear infinite;
    }
    
    .load-more-btn.loading .load-more-spinner {
      display: block;
    }
    
    .load-more-btn.loading .btn-icon {
      display: none;
    }
    
    @keyframes spin {
      to { transform: rotate(360deg); }
    }
    
    .load-more-message {
      text-align: center;
      margin-top: var(--spacing-3xl);
      padding: var(--spacing-xl);
      font-family: var(--font-mono);
      font-size: var(--text-sm);
      color: var(--text-secondary);
      opacity: 0;
      animation: fadeIn 0.5s ease-out forwards;
    }
    
    /* Fade-in animation for newly loaded posts */
    .post-card.ajax-loaded {
      opacity: 0;
      animation: fadeInUp 0.6s ease-out forwards;
    }
    
    /* RESPONSIVE DESIGN - MOBILE OPTIMIZATION */
    @media (max-width: 1024px) {
      .bento-large,
      .bento-medium,
      .bento-small {
        grid-column: span 12;
      }
      
      .bento-tall {
        grid-row: span 1;
      }
    }
    
    @media (max-width: 768px) {
      /* Hide desktop header, show mobile bottom nav */
      .header-section {
        display: none;
      }
      
      .mobile-bottom-nav {
        display: block;
      }
      
      /* Reduce orb sizes and complexity on mobile */
      .gradient-orb {
        width: 400px;
        height: 400px;
        filter: blur(80px);
        opacity: 0.2;
      }
      
      .gradient-orb-3 {
        display: none; /* Hide third orb on mobile */
      }
      
      /* Hero adjustments */
      .hero-container {
        min-height: 90vh;
        padding: var(--spacing-3xl) 0;
      }
      
      .hero-stats {
        flex-direction: column;
        gap: var(--spacing-lg);
        padding: var(--spacing-lg);
      }
      
      .stat-divider {
        width: 100%;
        height: 1px;
      }
      
      .hero-cta {
        flex-direction: column;
        width: 100%;
      }
      
      .hero-cta .magnetic-btn,
      .hero-cta .btn {
  width: 100%;
        justify-content: center;
      }
      
      /* Bento Grid mobile */
      .bento-grid {
        grid-template-columns: 1fr;
        gap: var(--spacing-md);
      }
      
      .bento-item {
        padding: var(--spacing-lg);
      }
      
      /* Typography scaling */
      .headline-premium {
        font-size: clamp(2rem, 8vw, 3rem);
      }
      
      /* Reduce magnetic effects on mobile for performance */
      .magnetic-btn:hover {
        transform: translateY(-2px) scale(1.01);
      }
    }
    
    @media (max-width: 480px) {
      .section {
        padding: var(--spacing-3xl) 0;
      }
      
      .container {
        padding: 0 var(--spacing-md);
      }
      
      .mobile-bottom-nav {
        width: calc(100% - 16px);
        padding: 8px 12px;
      }
      
      .mobile-nav-item {
        padding: 6px 8px;
        font-size: 0.625rem;
      }
      
      .mobile-nav-item svg {
        width: 18px;
        height: 18px;
      }
    }
    
    /* Responsive Blog Styles */
    @media (max-width: 1024px) {
      .posts-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: var(--spacing-lg);
      }
    }
    
    @media (max-width: 768px) {
      .posts-grid {
        grid-template-columns: 1fr;
        gap: var(--spacing-lg);
      }
      
      .post-thumbnail {
        height: 200px;
      }
      
      .post-content {
        padding: var(--spacing-lg);
      }
      
      .post-title {
        font-size: var(--text-lg);
      }
      
      .post-snippet {
        font-size: var(--text-sm);
      }
      
      .post-footer {
        flex-direction: column;
        align-items: flex-start;
      }
      
      .read-more-btn {
        width: 100%;
        justify-content: center;
      }
      
      .blog-pagination {
        flex-direction: column;
      }
      
      .pagination-btn {
        width: 100%;
        justify-content: center;
      }
    }
    
    /* Table of Contents Styles */
    .toc-container {
      position: fixed;
      top: 120px;
      right: 40px;
      width: 280px;
      max-height: calc(100vh - 160px);
      overflow-y: auto;
      padding: var(--spacing-xl);
      background: rgba(15, 15, 25, 0.7);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border: 1px solid rgba(59, 130, 246, 0.2);
      border-radius: var(--border-radius);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
      z-index: 100;
      opacity: 0;
      transform: translateX(20px);
      animation: tocFadeIn 0.5s ease-out 0.3s forwards;
      transition: all 0.3s ease;
    }
    
    @keyframes tocFadeIn {
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }
    
    .toc-container:hover {
      border-color: rgba(59, 130, 246, 0.4);
      box-shadow: 0 8px 40px rgba(59, 130, 246, 0.2);
    }
    
    .toc-header {
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
      margin-bottom: var(--spacing-lg);
      padding-bottom: var(--spacing-md);
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .toc-icon {
      color: var(--primary-color);
      flex-shrink: 0;
    }
    
    .toc-title {
      font-size: var(--text-sm);
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--text-primary);
      margin: 0;
    }
    
    .toc-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    
    .toc-item {
      margin: 0;
      padding: 0;
    }
    
    .toc-link {
      display: block;
      padding: var(--spacing-sm) var(--spacing-md);
      font-size: var(--text-sm);
      color: var(--text-secondary);
      text-decoration: none;
      border-left: 2px solid transparent;
      transition: all 0.2s ease;
      border-radius: 4px;
      line-height: 1.5;
    }
    
    .toc-link:hover {
      color: var(--primary-color);
      background: rgba(59, 130, 246, 0.1);
      border-left-color: var(--primary-color);
      padding-left: calc(var(--spacing-md) + 4px);
    }
    
    .toc-link.active {
      color: var(--primary-color);
      background: rgba(59, 130, 246, 0.15);
      border-left-color: var(--primary-color);
      font-weight: 600;
    }
    
    /* Nested H3 items */
    .toc-item .toc-list {
      margin-left: var(--spacing-md);
      margin-top: var(--spacing-xs);
    }
    
    .toc-item .toc-list .toc-link {
      font-size: var(--text-xs);
      padding: var(--spacing-xs) var(--spacing-md);
    }
    
    /* Custom scrollbar for TOC */
    .toc-container::-webkit-scrollbar {
      width: 6px;
    }
    
    .toc-container::-webkit-scrollbar-track {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 3px;
    }
    
    .toc-container::-webkit-scrollbar-thumb {
      background: rgba(59, 130, 246, 0.3);
      border-radius: 3px;
    }
    
    .toc-container::-webkit-scrollbar-thumb:hover {
      background: rgba(59, 130, 246, 0.5);
    }
    
    /* Hide TOC on tablet and mobile */
    @media (max-width: 1280px) {
      .toc-container {
        display: none;
      }
    }
    
    /* Empty state message */
    .toc-empty {
      font-size: var(--text-sm);
      color: var(--text-tertiary);
      font-style: italic;
      text-align: center;
      padding: var(--spacing-md) 0;
    }
    
    ]]></b:template-skin>
    
    <b:defaultmarkups>
      <b:defaultmarkup type='Common'>
        <b:includable id='widgetNotAvailableMessage'>
          <div style='padding: 20px; text-align: center; color: var(--text-secondary);'>
            <p>This widget is not available in preview mode.</p>
          </div>
        </b:includable>
      </b:defaultmarkup>
    </b:defaultmarkups>
    
    <!-- Prism.js Core Library -->
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/prism.min.js'></script>
    
    <!-- Prism.js Language Support -->
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-markup.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-css.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-javascript.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-typescript.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-jsx.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-tsx.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-python.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-bash.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-json.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-sql.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-markdown.min.js'></script>
    
    <!-- Prism.js Plugins -->
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/line-numbers/prism-line-numbers.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/toolbar/prism-toolbar.min.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/copy-to-clipboard/prism-copy-to-clipboard.min.js'></script>
    
    <!-- Prism.js Auto-Initialization -->
    <script>
    //<![CDATA[
    (function() {
      'use strict';
      
      window.Prism = window.Prism || {};
      Prism.manual = false;
      
      function highlightCode() {
        if (typeof Prism !== 'undefined' && Prism.highlightAll) {
          var codeBlocks = document.querySelectorAll('pre code, code[class*="language-"]');
          
          codeBlocks.forEach(function(block) {
            if (block.parentElement && block.parentElement.tagName === 'PRE') {
              block.parentElement.classList.add('line-numbers');
            }
            
            if (!block.className || block.className === '') {
              block.className = 'language-markup';
            }
          });
          
          Prism.highlightAll();
        }
      }
      
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', highlightCode);
      } else {
        highlightCode();
      }
      
      window.addEventListener('load', highlightCode);
    })();
    //]]>
    </script>
  </head>
  
  <body>
    <!-- Header Section -->
    <b:section class='header-section' id='header' maxwidgets='3' name='Header' showaddelement='yes'>
      <!-- Floating Navigation Widget -->
      <b:widget id='HTML99' locked='false' title='Floating Navigation' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<nav class="floating-nav">
  <a href="/" class="nav-brand">
    <div class="nav-brand-logo">M</div>
    <span>mishu.dev</span>
  </a>
  
  <ul class="nav-links" id="navLinks">
    <li><a href="#about" class="nav-link">About</a></li>
    <li><a href="#projects" class="nav-link">Ventures</a></li>
    <li><a href="#workflow" class="nav-link">AI Stack</a></li>
    <li><a href="#blog" class="nav-link">Blog</a></li>
    <li><a href="#contact" class="nav-link">Contact</a></li>
  </ul>
  
  <div style="display: flex; align-items: center; gap: 16px;">
    <a href="#connect" class="nav-cta hire-me-btn">Hire Me</a>
    <button class="theme-toggle" id="themeToggle" aria-label="Toggle theme">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="12" cy="12" r="5"/>
        <line x1="12" y1="1" x2="12" y2="3"/>
        <line x1="12" y1="21" x2="12" y2="23"/>
        <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
        <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
        <line x1="1" y1="12" x2="3" y2="12"/>
        <line x1="21" y1="12" x2="23" y2="12"/>
        <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
        <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
      </svg>
    </button>
    <button class="mobile-menu-toggle" id="mobileMenuToggle">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="3" y1="6" x2="21" y2="6"/>
        <line x1="3" y1="12" x2="21" y2="12"/>
        <line x1="3" y1="18" x2="21" y2="18"/>
      </svg>
    </button>
  </div>
</nav>

<!-- Mobile Bottom Navigation -->
<div class="mobile-bottom-nav" id="mobileBottomNav">
  <div class="mobile-nav-items">
    <a href="#about" class="mobile-nav-item">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
        <circle cx="12" cy="7" r="4"/>
      </svg>
      <span>About</span>
    </a>
    <a href="#projects" class="mobile-nav-item">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="3" y="3" width="18" height="18" rx="2"/>
        <line x1="9" y1="3" x2="9" y2="21"/>
      </svg>
      <span>Work</span>
    </a>
    <a href="/" class="mobile-nav-item active">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
        <polyline points="9 22 9 12 15 12 15 22"/>
      </svg>
      <span>Home</span>
    </a>
    <a href="#workflow" class="mobile-nav-item">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M13 2L3 14h10l-1 8 10-12H12l1-8z"/>
      </svg>
      <span>AI</span>
    </a>
    <a href="#contact" class="mobile-nav-item">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
        <polyline points="22,6 12,13 2,6"/>
      </svg>
      <span>Contact</span>
    </a>
  </div>
</div>

<script>
//<![CDATA[
// Floating header scroll effect
let lastScroll = 0;
const header = document.querySelector('.header-section');

window.addEventListener('scroll', () => {
  const currentScroll = window.pageYOffset;
  
  if (currentScroll > 50) {
    header.classList.add('scrolled');
  } else {
    header.classList.remove('scrolled');
  }
  
  lastScroll = currentScroll;
});

// Smooth scroll for nav links
document.querySelectorAll('.nav-link, .mobile-nav-item').forEach(link => {
  link.addEventListener('click', (e) => {
    const href = link.getAttribute('href');
    if (href.startsWith('#')) {
      e.preventDefault();
      const target = document.querySelector(href);
      if (target) {
        const offset = 100;
        const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - offset;
        window.scrollTo({
          top: targetPosition,
          behavior: 'smooth'
        });
      }
    }
  });
});

// Theme toggle
const themeToggle = document.getElementById('themeToggle');
themeToggle?.addEventListener('click', () => {
  document.body.classList.toggle('light-mode');
  // Save preference
  localStorage.setItem('theme', document.body.classList.contains('light-mode') ? 'light' : 'dark');
});

// Load theme preference
if (localStorage.getItem('theme') === 'light') {
  document.body.classList.add('light-mode');
}

// Smooth Scroll for Anchor Links
document.addEventListener('DOMContentLoaded', function() {
  // Select all anchor links (including CTAs and nav links)
  const anchorLinks = document.querySelectorAll('a[href^="#"]');
  
  anchorLinks.forEach(link => {
    link.addEventListener('click', function(e) {
      const href = this.getAttribute('href');
      
      // Skip if href is just "#" or empty
      if (!href || href === '#') return;
      
      e.preventDefault();
      
      // Get target element
      const targetId = href.substring(1);
      const targetElement = document.getElementById(targetId);
      
      if (targetElement) {
        // Calculate offset for fixed header
        const headerHeight = document.querySelector('.header-section')?.offsetHeight || 80;
        const targetPosition = targetElement.getBoundingClientRect().top + window.pageYOffset - headerHeight - 20;
        
        // Smooth scroll to target
        window.scrollTo({
          top: targetPosition,
          behavior: 'smooth'
        });
        
        // Close mobile menu if open
        const mobileNav = document.getElementById('mobileBottomNav');
        if (mobileNav && window.innerWidth <= 768) {
          mobileNav.classList.remove('active');
        }
      }
    });
  });
});
//]]>
</script>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- Hero Area Section -->
    <b:section class='hero-section section' id='hero' maxwidgets='3' name='Hero Area' showaddelement='yes'>
      <!-- Hero Widget -->
      <b:widget id='HTML1' locked='false' title='Hero Section' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<div class="hero-container">
  <!-- Animated Gradient Orbs -->
  <div class="gradient-orb gradient-orb-1"></div>
  <div class="gradient-orb gradient-orb-2"></div>
  <div class="gradient-orb gradient-orb-3"></div>
  
  <div class="container">
    <div class="hero-content scroll-reveal">
      <div class="hero-badge pulse-glow">
        <span class="badge-dot"></span>
        <span class="badge-text">Available for Projects</span>
      </div>
      
      <h1 class="hero-title headline-premium">
        <span class="hero-greeting">Hi, I'm</span>
        <span class="hero-name text-glow">Abu Md Selim</span>
      </h1>
      
      <div class="hero-roles scroll-reveal-2">
        <span class="role-item">Founder &amp; Visionary Lead</span>
        <span class="role-separator">•</span>
        <span class="role-item">Full-Stack Developer</span>
        <span class="role-separator">•</span>
        <span class="role-item">CSE Student</span>
      </div>
      
      <p class="hero-description scroll-reveal-3">
        Known as <strong>Mishu</strong> in the tech space, I'm building the future one venture at a time. 
        From freelancing success on Fiverr &amp; Upwork to founding <strong>Intactic Systems</strong>, 
        I empower businesses with bold, secure, and credible digital presence. Currently studying Computer Science at Premier University 
        while architecting hyperlocal solutions like <strong>The Chattala</strong> for Chittagong's digital transformation.
      </p>
      
      <div class="hero-cta scroll-reveal-4">
        <a href="#projects" class="magnetic-btn hover-glow">
          <span style="position: relative; z-index: 1;">Explore My Ventures</span>
          <svg style="position: relative; z-index: 1;" width="20" height="20" viewBox="0 0 20 20" fill="none">
            <path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
        <a href="https://intacticsys.com" target="_blank" rel="noopener" class="btn btn-secondary hover-lift">
          <span>Visit Intactic Systems</span>
        </a>
      </div>
      
      <div class="hero-stats glass-card hover-lift scroll-reveal-5">
        <div class="stat-item">
          <div class="stat-number">100+</div>
          <div class="stat-label">Fiverr/Upwork Projects</div>
        </div>
        <div class="stat-divider"></div>
        <div class="stat-item">
          <div class="stat-number">2</div>
          <div class="stat-label">Active Ventures</div>
        </div>
        <div class="stat-divider"></div>
        <div class="stat-item">
          <div class="stat-number">4</div>
          <div class="stat-label">AI Tools Mastered</div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
/*<![CDATA[*/
/* Hero Section Styles */
.hero-container {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
  padding: var(--spacing-4xl) 0;
}

/* Animated Background Gradient */
.hero-container::before {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: 
    radial-gradient(circle at 30% 40%, rgba(59, 130, 246, 0.15) 0%, transparent 40%),
    radial-gradient(circle at 70% 60%, rgba(139, 92, 246, 0.15) 0%, transparent 40%);
  animation: rotate 20s linear infinite;
  pointer-events: none;
}

@keyframes rotate {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

.hero-content {
  position: relative;
  z-index: var(--z-elevated);
  text-align: center;
  max-width: 900px;
  margin: 0 auto;
  animation: fadeInUp 1s ease-out;
}

/* Hero Badge */
.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  padding: var(--spacing-xs) var(--spacing-lg);
  background: rgba(16, 185, 129, 0.1);
  border: 1px solid rgba(16, 185, 129, 0.3);
  border-radius: 50px;
  margin-bottom: var(--spacing-xl);
  animation: pulse 2s ease-in-out infinite;
}

.badge-dot {
  width: 8px;
  height: 8px;
  background: var(--success-color);
  border-radius: 50%;
  box-shadow: 0 0 10px var(--success-color);
  animation: pulse-glow 2s ease-in-out infinite;
}

.badge-text {
  font-size: var(--text-sm);
  font-weight: 500;
  color: var(--success-color);
  font-family: var(--font-mono);
}

/* Hero Title */
.hero-title {
  font-size: clamp(2.5rem, 6vw, var(--text-6xl));
  font-weight: 800;
  margin-bottom: var(--spacing-lg);
  line-height: 1.1;
}

.hero-greeting {
  display: block;
  font-size: clamp(1.25rem, 2.5vw, var(--text-2xl));
  font-weight: 400;
  color: var(--text-secondary);
  margin-bottom: var(--spacing-sm);
}

.hero-name {
  display: block;
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 50%, var(--accent-color) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  position: relative;
}

/* Hero Roles */
.hero-roles {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-xl);
  flex-wrap: wrap;
}

.role-item {
  font-size: clamp(var(--text-base), 2vw, var(--text-lg));
  font-weight: 500;
  color: var(--text-primary);
  font-family: var(--font-mono);
}

.role-separator {
  color: var(--primary-color);
  font-weight: 700;
}

/* Hero Description */
.hero-description {
  font-size: clamp(var(--text-base), 2vw, var(--text-lg));
  color: var(--text-secondary);
  line-height: 1.8;
  margin-bottom: var(--spacing-2xl);
  max-width: 700px;
  margin-left: auto;
  margin-right: auto;
}

/* CTA Buttons */
.hero-cta {
  display: flex;
  gap: var(--spacing-lg);
  justify-content: center;
  margin-bottom: var(--spacing-3xl);
  flex-wrap: wrap;
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  padding: var(--spacing-md) var(--spacing-xl);
  font-size: var(--text-base);
  font-weight: 600;
  border-radius: var(--border-radius);
  transition: var(--transition);
  cursor: pointer;
  border: 2px solid transparent;
  text-decoration: none;
  font-family: var(--font-mono);
  position: relative;
  overflow: hidden;
}

.btn::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  transform: translate(-50%, -50%);
  transition: width 0.6s, height 0.6s;
}

.btn:hover::before {
  width: 300px;
  height: 300px;
}

.btn-primary {
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
  color: white;
  box-shadow: 0 4px 15px var(--primary-glow);
}

.btn-primary:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 25px var(--primary-glow);
  color: white;
}

.btn-secondary {
  background: transparent;
  color: var(--text-primary);
  border-color: var(--border-color);
}

.btn-secondary:hover {
  border-color: var(--primary-color);
  color: var(--primary-color);
  transform: translateY(-3px);
  box-shadow: 0 4px 15px var(--primary-glow);
}

.btn-icon {
  transition: transform 0.3s ease;
  position: relative;
  z-index: 1;
}

.btn:hover .btn-icon {
  transform: translateX(4px);
}

/* Hero Stats */
.hero-stats {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: var(--spacing-2xl);
  padding: var(--spacing-xl);
  border-radius: var(--border-radius-lg);
  flex-wrap: wrap;
}

.stat-item {
  text-align: center;
}

.stat-number {
  font-size: clamp(var(--text-2xl), 4vw, var(--text-3xl));
  font-weight: 700;
  color: var(--primary-color);
  margin-bottom: var(--spacing-xs);
  font-family: var(--font-mono);
}

.stat-label {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  font-weight: 500;
}

.stat-divider {
  width: 1px;
  height: 40px;
  background: var(--border-color);
}

/* Responsive */
@media (max-width: 768px) {
  .hero-container {
    min-height: auto;
    padding: var(--spacing-3xl) 0;
  }
  
  .hero-roles {
    gap: var(--spacing-sm);
  }
  
  .hero-cta {
    flex-direction: column;
    gap: var(--spacing-md);
    width: 100%;
  }
  
  .btn {
    width: 100%;
    justify-content: center;
  }
  
  .hero-stats {
    flex-direction: column;
    gap: var(--spacing-lg);
  }
  
  .stat-divider {
    display: none;
  }
}
/*]]>*/
</style>

<script>
//<![CDATA[
document.addEventListener('DOMContentLoaded', function() {
  const ctaButtons = document.querySelectorAll('.hero-cta a[href^="#"]');
  
  ctaButtons.forEach(button => {
    button.addEventListener('click', function(e) {
      const href = this.getAttribute('href');
      if (href && href.startsWith('#')) {
        e.preventDefault();
        const target = document.querySelector(href);
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      }
    });
  });
});
//]]>
</script>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- About Me Section -->
    <b:section class='about-section section bg-secondary' id='about' maxwidgets='3' name='About Me' showaddelement='yes'>
      <!-- AI Workflow Widget -->
      <b:widget id='HTML4' locked='false' title='AI-Powered Development Workflow' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<div class="ai-workflow-container">
  <div class="container">
    <div class="section-header scroll-reveal">
      <span class="section-label">Tech Stack</span>
      <h2 class="section-title headline-premium">AI-Powered Development Workflow</h2>
      <p class="section-description">
        Leveraging cutting-edge AI tools to supercharge development velocity, code quality, and system architecture. 
        Each tool serves a strategic purpose in my workflow.
      </p>
    </div>
    
    <div class="ai-tools-grid">
      <!-- Cursor AI -->
      <div class="ai-tool-card glass-card hover-lift scroll-reveal-1" data-tool="1">
        <div class="tool-header">
          <div class="tool-logo">
            <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
              <rect width="48" height="48" rx="12" fill="url(#cursorGradient)"/>
              <path d="M16 12L16 36L24 28L32 36V12H16Z" fill="white" stroke="white" stroke-width="2" stroke-linejoin="round"/>
              <circle cx="24" cy="20" r="3" fill="#3b82f6"/>
              <defs>
                <linearGradient id="cursorGradient" x1="0" y1="0" x2="48" y2="48">
                  <stop offset="0%" stop-color="#3b82f6"/>
                  <stop offset="100%" stop-color="#1e40af"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          <div class="tool-title-group">
            <h3 class="tool-name">Cursor AI</h3>
            <span class="tool-badge">IDE</span>
          </div>
        </div>
        
        <div class="tool-content">
          <p class="tool-description">
            My primary development environment. <strong>Cursor transforms VSCode into an AI-first IDE.</strong> 
            I use it for rapid prototyping, generating boilerplate code, and real-time pair programming. 
            The context-aware suggestions feel like having a senior developer looking over my shoulder 24/7.
          </p>
          
          <div class="tool-use-cases">
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Rapid UI prototyping</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Boilerplate generation</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Real-time code review</span>
            </div>
          </div>
        </div>
      </div>
      
      <!-- GitHub Copilot -->
      <div class="ai-tool-card glass-card hover-lift scroll-reveal-2" data-tool="2">
        <div class="tool-header">
          <div class="tool-logo">
            <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
              <rect width="48" height="48" rx="12" fill="#24292e"/>
              <path d="M24 14C18.5 14 14 18.5 14 24C14 28.4 16.9 32.2 21 33.5V31C21 31 20.5 31 20 31C18.3 31 17.5 29.8 17.2 29C17 28.5 16.5 28 16 28C15.7 28 15.5 28.2 15.5 28.5C15.5 28.8 15.7 29 16 29C16.5 29 17 29.5 17.2 30C17.5 30.8 18.3 32 20 32C20.5 32 21 32 21 32V35.8C26.4 35.1 30.5 30.5 30.5 25C30.5 19 26 14.5 20.5 14.5L24 14Z" fill="white"/>
              <circle cx="28" cy="19" r="3" fill="#6e40c9" stroke="white" stroke-width="1.5"/>
            </svg>
          </div>
          <div class="tool-title-group">
            <h3 class="tool-name">GitHub Copilot</h3>
            <span class="tool-badge">Assistant</span>
          </div>
        </div>
        
        <div class="tool-content">
          <p class="tool-description">
            <strong>The OG AI coding assistant.</strong> Copilot handles the heavy lifting of repetitive patterns, 
            test generation, and documentation. It's trained on billions of lines of code, making it invaluable for 
            idiomatic implementations and discovering best practices I might have missed.
          </p>
          
          <div class="tool-use-cases">
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Function autocomplete</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Test suite generation</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Documentation writing</span>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Claude -->
      <div class="ai-tool-card glass-card hover-lift scroll-reveal-3" data-tool="3">
        <div class="tool-header">
          <div class="tool-logo">
            <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
              <rect width="48" height="48" rx="12" fill="url(#claudeGradient)"/>
              <circle cx="24" cy="18" r="5" fill="white"/>
              <rect x="14" y="26" width="20" height="14" rx="2" fill="white"/>
              <rect x="18" y="30" width="4" height="6" fill="#c17767"/>
              <rect x="26" y="30" width="4" height="6" fill="#c17767"/>
              <defs>
                <linearGradient id="claudeGradient" x1="0" y1="0" x2="48" y2="48">
                  <stop offset="0%" stop-color="#d4a574"/>
                  <stop offset="100%" stop-color="#c17767"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          <div class="tool-title-group">
            <h3 class="tool-name">Claude (Anthropic)</h3>
            <span class="tool-badge">Architect</span>
          </div>
        </div>
        
        <div class="tool-content">
          <p class="tool-description">
            <strong>The brain behind complex system design.</strong> When I need to refactor a massive codebase, 
            design a scalable architecture, or reason through intricate technical decisions, Claude's 200K context window 
            is unmatched. It understands nuance, catches edge cases, and thinks several steps ahead.
          </p>
          
          <div class="tool-use-cases">
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>System architecture design</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Large-scale refactoring</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Technical documentation</span>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Devin -->
      <div class="ai-tool-card glass-card hover-lift scroll-reveal-4" data-tool="4">
        <div class="tool-header">
          <div class="tool-logo">
            <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
              <rect width="48" height="48" rx="12" fill="url(#devinGradient)"/>
              <rect x="12" y="12" width="24" height="24" rx="2" stroke="white" stroke-width="2" fill="none"/>
              <path d="M12 18H36M18 12V36M24 12V36M30 12V36" stroke="white" stroke-width="2"/>
              <circle cx="24" cy="24" r="4" fill="white"/>
              <defs>
                <linearGradient id="devinGradient" x1="0" y1="0" x2="48" y2="48">
                  <stop offset="0%" stop-color="#7c3aed"/>
                  <stop offset="100%" stop-color="#4c1d95"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          <div class="tool-title-group">
            <h3 class="tool-name">Devin AI</h3>
            <span class="tool-badge">Autonomous</span>
          </div>
        </div>
        
        <div class="tool-content">
          <p class="tool-description">
            <strong>The autonomous software engineer.</strong> Devin doesn't just suggest code—it executes entire tasks end-to-end. 
            From setting up CI/CD pipelines to debugging production issues across multiple services, Devin operates independently 
            while I focus on strategic decisions. It's like having a junior dev who never sleeps.
          </p>
          
          <div class="tool-use-cases">
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>End-to-end feature development</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>DevOps automation</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Bug hunting &amp; fixes</span>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Gemini Pro -->
      <div class="ai-tool-card glass-card hover-lift scroll-reveal-5" data-tool="5">
        <div class="tool-header">
          <div class="tool-logo">
            <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
              <rect width="48" height="48" rx="12" fill="url(#geminiGradient)"/>
              <path d="M24 8L30 18L40 20L32 28L34 40L24 34L14 40L16 28L8 20L18 18L24 8Z" fill="white" stroke="white" stroke-width="2" stroke-linejoin="round"/>
              <circle cx="24" cy="24" r="6" fill="#4285f4"/>
              <defs>
                <linearGradient id="geminiGradient" x1="0" y1="0" x2="48" y2="48">
                  <stop offset="0%" stop-color="#4285f4"/>
                  <stop offset="50%" stop-color="#ea4335"/>
                  <stop offset="100%" stop-color="#fbbc04"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          <div class="tool-title-group">
            <h3 class="tool-name">Gemini Pro</h3>
            <span class="tool-badge">Research</span>
          </div>
        </div>
        
        <div class="tool-content">
          <p class="tool-description">
            <strong>My research and strategy partner.</strong> Gemini Pro excels at dynamic research, competitive analysis, 
            and business modeling. When I'm structuring case studies, exploring market opportunities, or validating technical approaches, 
            Gemini's multimodal capabilities and real-time data access make it indispensable.
          </p>
          
          <div class="tool-use-cases">
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Market research</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Business modeling</span>
            </div>
            <div class="use-case">
              <svg class="check-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path d="M16 6L8.5 13.5L4 9" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Case study structuring</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <div class="ai-workflow-footer">
      <div class="workflow-note glass-card">
        <svg class="note-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
          <path d="M13 2L3 14H12L11 22L21 10H12L13 2Z" fill="#fbbf24" stroke="#fbbf24" stroke-width="2" stroke-linejoin="round"/>
        </svg>
        <p>
          <strong>The Future is AI-Augmented:</strong> These tools don't replace developers—they amplify us. 
          By automating the mundane and accelerating the complex, I can focus on what truly matters: 
          solving real problems, building authentic relationships, and creating meaningful impact.
        </p>
      </div>
    </div>
  </div>
</div>

<style>
/*<![CDATA[*/
/* AI Workflow Section */
.ai-workflow-container {
  padding: var(--spacing-4xl) 0;
}

.ai-tools-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: var(--spacing-xl);
  margin-bottom: var(--spacing-3xl);
}

.ai-tool-card {
  padding: var(--spacing-xl);
  transition: var(--transition);
  animation: fadeInUp 0.8s ease-out backwards;
  display: flex;
  flex-direction: column;
}

.ai-tool-card[data-tool="1"] { animation-delay: 0.1s; }
.ai-tool-card[data-tool="2"] { animation-delay: 0.2s; }
.ai-tool-card[data-tool="3"] { animation-delay: 0.3s; }
.ai-tool-card[data-tool="4"] { animation-delay: 0.4s; }
.ai-tool-card[data-tool="5"] { animation-delay: 0.5s; }

.ai-tool-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 12px 40px rgba(59, 130, 246, 0.3);
  border-color: rgba(59, 130, 246, 0.4);
}

.tool-header {
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-lg);
  padding-bottom: var(--spacing-lg);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.tool-logo {
  flex-shrink: 0;
}

.tool-title-group {
  flex: 1;
}

.tool-name {
  font-size: var(--text-xl);
  font-weight: 700;
  color: var(--text-primary);
  margin: 0 0 var(--spacing-xs) 0;
}

.tool-badge {
  display: inline-block;
  padding: var(--spacing-xs) var(--spacing-sm);
  background: rgba(59, 130, 246, 0.15);
  color: var(--primary-color);
  font-size: var(--text-xs);
  font-weight: 600;
  font-family: var(--font-mono);
  border-radius: 4px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.tool-content {
  flex: 1;
}

.tool-description {
  font-size: var(--text-base);
  line-height: 1.7;
  color: var(--text-secondary);
  margin-bottom: var(--spacing-lg);
}

.tool-description strong {
  color: var(--text-primary);
  font-weight: 600;
}

.tool-use-cases {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
}

.use-case {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  font-size: var(--text-sm);
  color: var(--text-secondary);
  font-family: var(--font-mono);
}

.check-icon {
  flex-shrink: 0;
}

.ai-workflow-footer {
  margin-top: var(--spacing-2xl);
}

.workflow-note {
  padding: var(--spacing-xl);
  display: flex;
  gap: var(--spacing-lg);
  align-items: flex-start;
  border-left: 3px solid #fbbf24;
}

.note-icon {
  flex-shrink: 0;
  margin-top: var(--spacing-xs);
}

.workflow-note p {
  font-size: var(--text-base);
  line-height: 1.7;
  color: var(--text-secondary);
  margin: 0;
}

.workflow-note strong {
  color: var(--text-primary);
  font-weight: 600;
}

/* Status dots for project cards */
.status-development {
  background: #fbbf24;
  box-shadow: 0 0 8px #fbbf24;
}

/* Responsive */
@media (max-width: 768px) {
  .ai-tools-grid {
    grid-template-columns: 1fr;
  }
  
  .tool-header {
    flex-direction: column;
    align-items: flex-start;
  }
  
  .workflow-note {
    flex-direction: column;
  }
}
/*]]>*/
</style>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- Skills Section -->
    <b:section class='skills-section section' id='skills' maxwidgets='5' name='Skills' showaddelement='yes'>
      <!-- Skills Widget -->
      <b:widget id='HTML3' locked='false' title='Technical Skills' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<div class="skills-container">
  <div class="container">
    <div class="section-header scroll-reveal">
      <span class="section-label">Expertise</span>
      <h2 class="section-title headline-premium">Technical Skills &amp; Tools</h2>
      <p class="section-description">
        Technologies and frameworks I work with to build modern digital solutions
      </p>
    </div>
    
    <div class="skills-wrapper">
      <div class="skill-category glass-card hover-lift scroll-reveal-1">
        <h3 class="category-title">
          <svg class="category-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
            <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M2 17L12 22L22 17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M2 12L12 17L22 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          Frontend Development
        </h3>
        <div class="skills-grid">
          <span class="skill-tag" data-level="expert">Next.js</span>
          <span class="skill-tag" data-level="expert">React</span>
          <span class="skill-tag" data-level="expert">Tailwind CSS</span>
          <span class="skill-tag" data-level="advanced">TypeScript</span>
          <span class="skill-tag" data-level="advanced">JavaScript</span>
          <span class="skill-tag" data-level="advanced">HTML5/CSS3</span>
        </div>
      </div>
      
      <div class="skill-category glass-card hover-lift scroll-reveal-2">
        <h3 class="category-title">
          <svg class="category-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
            <ellipse cx="12" cy="5" rx="9" ry="3" stroke="currentColor" stroke-width="2"/>
            <path d="M21 12C21 13.66 16.97 15 12 15C7.03 15 3 13.66 3 12" stroke="currentColor" stroke-width="2"/>
            <path d="M3 5V19C3 20.66 7.03 22 12 22C16.97 22 21 20.66 21 19V5" stroke="currentColor" stroke-width="2"/>
          </svg>
          Backend &amp; Database
        </h3>
        <div class="skills-grid">
          <span class="skill-tag" data-level="expert">Prisma</span>
          <span class="skill-tag" data-level="expert">Supabase</span>
          <span class="skill-tag" data-level="advanced">Node.js</span>
          <span class="skill-tag" data-level="advanced">PostgreSQL</span>
          <span class="skill-tag" data-level="intermediate">MongoDB</span>
          <span class="skill-tag" data-level="advanced">REST APIs</span>
        </div>
      </div>
      
      <div class="skill-category glass-card hover-lift scroll-reveal-3">
        <h3 class="category-title">
          <svg class="category-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
            <path d="M18 10H16.74C16.38 8.67 15.64 7.47 14.62 6.55L15.46 5.71L14.04 4.29L13.2 5.13C12.28 4.45 11.18 4 10 4C8.82 4 7.72 4.45 6.8 5.13L5.96 4.29L4.54 5.71L5.38 6.55C4.36 7.47 3.62 8.67 3.26 10H2V12H3.26C3.62 13.33 4.36 14.53 5.38 15.45L4.54 16.29L5.96 17.71L6.8 16.87C7.72 17.55 8.82 18 10 18C11.18 18 12.28 17.55 13.2 16.87L14.04 17.71L15.46 16.29L14.62 15.45C15.64 14.53 16.38 13.33 16.74 12H18V10Z" fill="currentColor" fill-opacity="0.2" stroke="currentColor" stroke-width="1.5"/>
          </svg>
          Cloud &amp; DevOps
        </h3>
        <div class="skills-grid">
          <span class="skill-tag" data-level="advanced">Vercel</span>
          <span class="skill-tag" data-level="advanced">AWS</span>
          <span class="skill-tag" data-level="intermediate">Docker</span>
          <span class="skill-tag" data-level="intermediate">CI/CD</span>
          <span class="skill-tag" data-level="advanced">Git</span>
          <span class="skill-tag" data-level="advanced">GitHub</span>
        </div>
      </div>
      
      <div class="skill-category glass-card hover-lift scroll-reveal-4">
        <h3 class="category-title">
          <svg class="category-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
            <rect x="3" y="3" width="18" height="18" rx="2" stroke="currentColor" stroke-width="2"/>
            <path d="M3 9H21M9 21V9" stroke="currentColor" stroke-width="2"/>
          </svg>
          Additional Skills
        </h3>
        <div class="skills-grid">
          <span class="skill-tag" data-level="expert">Web Development</span>
          <span class="skill-tag" data-level="advanced">UI/UX Design</span>
          <span class="skill-tag" data-level="advanced">Responsive Design</span>
          <span class="skill-tag" data-level="intermediate">SEO</span>
          <span class="skill-tag" data-level="advanced">API Integration</span>
          <span class="skill-tag" data-level="intermediate">Testing</span>
        </div>
      </div>
    </div>
    
    <div class="skills-footer">
      <p class="skills-note">
        💡 <strong>Always Learning:</strong> Continuously exploring new technologies and best practices
      </p>
    </div>
  </div>
</div>

<style>
/*<![CDATA[*/
/* Skills Section */
.skills-container {
  padding: var(--spacing-4xl) 0;
}

.skills-wrapper {
  display: grid;
  gap: var(--spacing-xl);
  margin-bottom: var(--spacing-2xl);
}

.skill-category {
  padding: var(--spacing-xl);
  transition: var(--transition);
  animation: fadeInUp 0.8s ease-out backwards;
}

.skill-category:nth-child(1) { animation-delay: 0.1s; }
.skill-category:nth-child(2) { animation-delay: 0.2s; }
.skill-category:nth-child(3) { animation-delay: 0.3s; }
.skill-category:nth-child(4) { animation-delay: 0.4s; }

.skill-category:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 30px rgba(59, 130, 246, 0.2);
  border-color: rgba(59, 130, 246, 0.3);
}

.category-title {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  font-size: var(--text-xl);
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: var(--spacing-lg);
}

.category-icon {
  color: var(--primary-color);
  flex-shrink: 0;
}

.skills-grid {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-sm);
}

/* Tech Tags with Monospace Font */
.skill-tag {
  position: relative;
  display: inline-flex;
  align-items: center;
  padding: var(--spacing-sm) var(--spacing-lg);
  background: rgba(59, 130, 246, 0.1);
  color: var(--text-primary);
  font-size: var(--text-sm);
  font-weight: 500;
  font-family: var(--font-mono);
  border-radius: 50px;
  border: 1px solid rgba(59, 130, 246, 0.2);
  transition: var(--transition);
  cursor: default;
  overflow: hidden;
}

.skill-tag::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  height: 3px;
  background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
  transition: width 0.3s ease;
}

.skill-tag[data-level="expert"]::before {
  width: 100%;
}

.skill-tag[data-level="advanced"]::before {
  width: 75%;
}

.skill-tag[data-level="intermediate"]::before {
  width: 50%;
}

.skill-tag:hover {
  background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
  color: white;
  border-color: var(--primary-color);
  transform: translateY(-2px) scale(1.05);
  box-shadow: 0 4px 15px var(--primary-glow);
}

.skill-tag[data-level="expert"]:hover {
  box-shadow: 0 4px 20px var(--primary-glow), 0 0 30px var(--primary-glow);
}

.skills-footer {
  text-align: center;
  padding-top: var(--spacing-xl);
  border-top: 1px solid var(--border-color);
}

.skills-note {
  font-size: var(--text-base);
  color: var(--text-secondary);
  line-height: 1.6;
}

.skills-note strong {
  color: var(--primary-color);
}

@media (max-width: 768px) {
  .skill-category {
    padding: var(--spacing-lg);
  }
  
  .category-title {
    font-size: var(--text-lg);
  }
  
  .skill-tag {
    font-size: var(--text-xs);
    padding: var(--spacing-xs) var(--spacing-md);
  }
}
/*]]>*/
</style>

<script>
//<![CDATA[
document.addEventListener('DOMContentLoaded', function() {
  const skillTags = document.querySelectorAll('.skill-tag');
  
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.animation = 'fadeInUp 0.5s ease-out';
      }
    });
  }, {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  });
  
  document.querySelectorAll('.skill-category').forEach(category => {
    observer.observe(category);
  });
});
//]]>
</script>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- Main Blog Section (Case Studies) -->
    <b:section class='main-section section bg-secondary' id='main' maxwidgets='1' name='Blog Posts (Case Studies)' showaddelement='yes'>
      <b:widget id='Blog1' locked='false' title='Case Studies' type='Blog' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='showTitle'>true</b:widget-setting>
          <b:widget-setting name='showPostFooter'>false</b:widget-setting>
        </b:widget-settings>
        
        <b:includable id='main' var='this'>
          <div class='blog-container'>
            <div class='container'>
              <div class='section-header scroll-reveal'>
                <span class='section-label'>Latest Work</span>
                <h2 class='section-title headline-premium'>Case Studies &amp; Articles</h2>
                <p class='section-description'>
                  Detailed insights into projects, technical deep-dives, and industry perspectives
                </p>
              </div>
              
              <div class='posts-grid'>
                <b:loop values='data:posts' var='post'>
                  <b:include name='post' data='post'/>
                </b:loop>
              </div>
              
              <b:include name='postPagination'/>
            </div>
          </div>
        </b:includable>
        
        <b:includable id='post' var='post'>
          <article class='post-card glass-card hover-lift'>
            <b:if cond='data:post.firstImageUrl'>
              <div class='post-thumbnail'>
                <a expr:href='data:post.url' expr:title='data:post.title'>
                  <img expr:alt='data:post.title' expr:src='data:post.firstImageUrl' loading='lazy'/>
                  <div class='post-overlay'>
                    <svg class='overlay-icon' width='48' height='48' viewBox='0 0 48 48' fill='none'>
                      <circle cx='24' cy='24' r='24' fill='rgba(59, 130, 246, 0.3)'/>
                      <path d='M19 14L29 24L19 34' stroke='white' stroke-width='3' stroke-linecap='round' stroke-linejoin='round'/>
                    </svg>
                  </div>
                </a>
              </div>
            </b:if>
            
            <div class='post-content'>
              <b:if cond='data:post.labels'>
                <div class='post-labels'>
                  <b:loop values='data:post.labels limit 2' var='label'>
                    <a class='post-label' expr:href='data:label.url' rel='tag'>
                      <data:label.name/>
                    </a>
                  </b:loop>
                </div>
              </b:if>
              
              <h3 class='post-title'>
                <a expr:href='data:post.url' expr:title='data:post.title'>
                  <data:post.title/>
                </a>
              </h3>
              
              <!-- Reading Time Display -->
              <div class='post-reading-time' expr:data-post-id='data:post.id'>
                <svg width='16' height='16' viewBox='0 0 16 16' fill='none'>
                  <circle cx='8' cy='8' r='6' stroke='currentColor' stroke-width='1.5'/>
                  <path d='M8 4V8L11 11' stroke='currentColor' stroke-width='1.5' stroke-linecap='round'/>
                </svg>
                <span class='reading-time-text'>Calculating...</span>
              </div>
              
              <b:if cond='data:post.snippet'>
                <p class='post-snippet'>
                  <data:post.snippet/>
                </p>
              </b:if>
              
              <div class='post-footer'>
                <a class='read-more-btn' expr:href='data:post.url'>
                  <span>Read Case Study</span>
                  <svg width='20' height='20' viewBox='0 0 20 20' fill='none'>
                    <path d='M7.5 15L12.5 10L7.5 5' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
                  </svg>
                </a>
                
                <span class='post-meta'>
                  <svg width='16' height='16' viewBox='0 0 16 16' fill='none'>
                    <circle cx='8' cy='8' r='6' stroke='currentColor' stroke-width='1.5'/>
                    <path d='M8 4V8L11 11' stroke='currentColor' stroke-width='1.5' stroke-linecap='round'/>
                  </svg>
                  <span>5 min read</span>
                </span>
              </div>
            </div>
          </article>
        </b:includable>
        
        <b:includable id='postPagination'>
          <b:if cond='data:posts.size != 0'>
            <div class='load-more-container'>
              <b:if cond='data:olderPageUrl'>
                <button class='load-more-btn' expr:data-next-url='data:olderPageUrl' id='load-more-posts'>
                  <div class='load-more-spinner'></div>
                  <svg class='btn-icon' width='20' height='20' viewBox='0 0 20 20' fill='none'>
                    <path d='M10 4V16M10 16L6 12M10 16L14 12' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
                  </svg>
                  <span class='btn-text'>Load More</span>
                </button>
              </b:if>
            </div>
          </b:if>
        </b:includable>
        
        <b:includable id='previousPageLink'/>
        <b:includable id='nextPageLink'/>
        
        <b:includable id='post-share-buttons'/>
        <b:includable id='postFooter'/>
        <b:includable id='postAuthor'/>
        <b:includable id='postTimestamp'/>
        <b:includable id='feedLinks'/>
      </b:widget>
    </b:section>
    
    <!-- Projects/Ventures Section -->
    <b:section class='projects-section section' id='projects' maxwidgets='6' name='Projects/Ventures' showaddelement='yes'>
      <b:widget id='HTML2' locked='false' title='Featured Projects' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<div class="projects-container">
  <div class="container">
    <div class="section-header">
      <span class="section-label">Portfolio</span>
      <h2 class="section-title headline-premium">Featured Projects &amp; Ventures</h2>
      <p class="section-description">
        Building the future through strategic ventures and cutting-edge technology
      </p>
    </div>
    
    <!-- Bento Grid Layout for Projects -->
    <div class="bento-grid">
      <div class="bento-item bento-large magnetic-card hover-glow scroll-reveal-1" data-project="1">
        <div class="project-card-inner">
          <div class="project-icon">
            <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
              <rect width="40" height="40" rx="8" fill="url(#gradient1)"/>
              <path d="M20 10L12 14V20C12 25 20 30 20 30C20 30 28 25 28 20V14L20 10Z" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M20 20L16 18V22L20 24L24 22V18L20 20Z" fill="white" opacity="0.6"/>
              <defs>
                <linearGradient id="gradient1" x1="0" y1="0" x2="40" y2="40">
                  <stop offset="0%" stop-color="#10b981"/>
                  <stop offset="100%" stop-color="#3b82f6"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          
          <div class="project-badge">Parent Company</div>
          
          <h3 class="project-title">Intactic Systems</h3>
          
          <p class="project-description">
            <strong>My main venture.</strong> Empowering Businesses &amp; Professionals through a bold, secure, and credible online presence. 
            Full-service IT consulting, custom software development, and digital transformation. From startups to enterprises, we architect solutions that scale.
          </p>
          
          <div class="project-tech">
            <span class="tech-tag">React</span>
            <span class="tech-tag">Node.js</span>
            <span class="tech-tag">AWS</span>
            <span class="tech-tag">Docker</span>
            <span class="tech-tag">PostgreSQL</span>
          </div>
          
          <div class="project-footer">
            <a href="https://intacticsys.com" class="project-link magnetic-btn" target="_blank" rel="noopener" style="padding: 12px 24px; font-size: 0.9rem;">
              <span style="position: relative; z-index: 1;">Visit Website</span>
              <svg style="position: relative; z-index: 1;" width="16" height="16" viewBox="0 0 16 16" fill="none">
                <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
            <div class="project-status">
              <span class="status-dot status-live"></span>
              <span class="status-text">Active</span>
            </div>
          </div>
        </div>
      </div>
      
      <div class="bento-item bento-medium bento-tall magnetic-card hover-glow scroll-reveal-2" data-project="2">
        <div class="project-card-inner">
          <div class="project-icon">
            <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
              <rect width="40" height="40" rx="8" fill="url(#gradient2)"/>
              <path d="M20 8C13.4 8 8 13.4 8 20C8 26.6 13.4 32 20 32C26.6 32 32 26.6 32 20C32 13.4 26.6 8 20 8Z" stroke="white" stroke-width="2" fill="none"/>
              <path d="M20 8C20 8 24 13 24 20C24 27 20 32 20 32M20 8C20 8 16 13 16 20C16 27 20 32 20 32" stroke="white" stroke-width="2" stroke-linecap="round"/>
              <path d="M8 20H32M10 14H30M10 26H30" stroke="white" stroke-width="2" stroke-linecap="round"/>
              <defs>
                <linearGradient id="gradient2" x1="0" y1="0" x2="40" y2="40">
                  <stop offset="0%" stop-color="#3b82f6"/>
                  <stop offset="100%" stop-color="#8b5cf6"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          
          <div class="project-badge">Flagship Product</div>
          
          <h3 class="project-title">The Chattala</h3>
          
          <p class="project-description">
            <strong>A dream project under Intactic Systems.</strong> Hyperlocal digital platform revolutionizing Chittagong's community engagement. 
            Integrating community hubs, local service directories, e-commerce, and real-time neighborhood connections.
          </p>
          
          <div class="project-tech">
            <span class="tech-tag">Next.js</span>
            <span class="tech-tag">Prisma</span>
            <span class="tech-tag">Tailwind</span>
            <span class="tech-tag">Redis</span>
          </div>
          
          <div class="project-footer">
            <a href="#" class="project-link" target="_blank" rel="noopener">
              <span>Coming Soon</span>
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
            <div class="project-status">
              <span class="status-dot status-development"></span>
              <span class="status-text">In Development</span>
            </div>
          </div>
        </div>
      </div>
      
      <div class="bento-item bento-medium magnetic-card hover-glow scroll-reveal-3" data-project="3">
        <div class="project-card-inner">
          <div class="project-icon">
            <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
              <rect width="40" height="40" rx="8" fill="url(#gradient3)"/>
              <path d="M13 28L20 12L27 28" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M16 22H24" stroke="white" stroke-width="2.5" stroke-linecap="round"/>
              <circle cx="20" cy="12" r="2" fill="white"/>
              <defs>
                <linearGradient id="gradient3" x1="0" y1="0" x2="40" y2="40">
                  <stop offset="0%" stop-color="#8b5cf6"/>
                  <stop offset="100%" stop-color="#06b6d4"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          
          <div class="project-badge">Portfolio</div>
          
          <h3 class="project-title">mishu.dev</h3>
          
          <p class="project-description">
            My personal digital space where ideas meet execution. Technical deep-dives, case studies, and thoughts on tech entrepreneurship. 
            Built from scratch pushing Blogger platform boundaries.
          </p>
          
          <div class="project-tech">
            <span class="tech-tag">Blogger XML</span>
            <span class="tech-tag">Vanilla JS</span>
            <span class="tech-tag">CSS3</span>
            <span class="tech-tag">Prism.js</span>
          </div>
          
          <div class="project-footer">
            <a href="https://mishu.dev" class="project-link hover-lift" target="_blank" rel="noopener">
              <span>You're Here!</span>
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </a>
            <div class="project-status">
              <span class="status-dot status-live"></span>
              <span class="status-text">Live</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <div class="projects-cta">
      <a href="#all-projects" class="link-with-arrow">
        <span>View All Projects</span>
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>
  </div>
</div>

<style>
/*<![CDATA[*/
/* Projects Section */
.projects-container {
  padding: var(--spacing-4xl) 0;
}

.projects-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: var(--spacing-xl);
  margin-bottom: var(--spacing-2xl);
}

.project-card {
  overflow: hidden;
  transition: var(--transition);
  animation: fadeInUp 0.8s ease-out backwards;
  display: flex;
  flex-direction: column;
  height: 100%;
}

.project-card[data-project="1"] { animation-delay: 0.1s; }
.project-card[data-project="2"] { animation-delay: 0.2s; }
.project-card[data-project="3"] { animation-delay: 0.3s; }

.project-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: linear-gradient(90deg, var(--primary-color), var(--secondary-color), var(--accent-color));
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.3s ease;
}

.project-card:hover::before {
  transform: scaleX(1);
}

.project-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 12px 40px rgba(59, 130, 246, 0.3);
  border-color: rgba(59, 130, 246, 0.4);
}

.project-card-inner {
  padding: var(--spacing-xl);
  display: flex;
  flex-direction: column;
  height: 100%;
}

.project-icon {
  margin-bottom: var(--spacing-lg);
}

.project-badge {
  display: inline-block;
  align-self: flex-start;
  padding: var(--spacing-xs) var(--spacing-md);
  background: rgba(59, 130, 246, 0.1);
  color: var(--primary-color);
  font-size: var(--text-xs);
  font-weight: 600;
  font-family: var(--font-mono);
  text-transform: uppercase;
  letter-spacing: 1px;
  border-radius: var(--border-radius);
  border: 1px solid rgba(59, 130, 246, 0.2);
  margin-bottom: var(--spacing-md);
}

.project-title {
  font-size: var(--text-2xl);
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: var(--spacing-md);
  line-height: 1.3;
}

.project-description {
  font-size: var(--text-base);
  color: var(--text-secondary);
  line-height: 1.7;
  margin-bottom: var(--spacing-lg);
  flex-grow: 1;
}

.project-tech {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-xs);
  margin-bottom: var(--spacing-lg);
}

.tech-tag {
  display: inline-block;
  padding: var(--spacing-xs) var(--spacing-md);
  background: rgba(139, 92, 246, 0.1);
  color: var(--secondary-color);
  font-size: var(--text-xs);
  font-weight: 600;
  font-family: var(--font-mono);
  border-radius: var(--border-radius);
  border: 1px solid rgba(139, 92, 246, 0.2);
  transition: var(--transition);
}

.tech-tag:hover {
  background: var(--secondary-color);
  color: white;
  border-color: var(--secondary-color);
  box-shadow: 0 4px 15px var(--secondary-glow);
}

.project-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: var(--spacing-md);
  border-top: 1px solid var(--border-color);
  gap: var(--spacing-md);
}

.project-link {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  font-size: var(--text-sm);
  font-weight: 600;
  font-family: var(--font-mono);
  color: var(--primary-color);
  text-decoration: none;
  transition: var(--transition);
}

.project-link:hover {
  gap: var(--spacing-sm);
  color: var(--secondary-color);
}

.project-link svg {
  transition: transform 0.3s ease;
}

.project-link:hover svg {
  transform: translateX(4px);
}

.project-status {
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
}

.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  animation: pulse-glow 2s ease-in-out infinite;
}

.status-dot.status-live {
  background: var(--success-color);
  box-shadow: 0 0 10px var(--success-color);
}

.status-text {
  font-size: var(--text-xs);
  color: var(--text-secondary);
  font-weight: 500;
  font-family: var(--font-mono);
}

.projects-cta {
  text-align: center;
  margin-top: var(--spacing-2xl);
}

.link-with-arrow {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-sm);
  font-size: var(--text-lg);
  font-weight: 600;
  font-family: var(--font-mono);
  color: var(--primary-color);
  text-decoration: none;
  transition: var(--transition);
}

.link-with-arrow:hover {
  color: var(--secondary-color);
  gap: var(--spacing-md);
}

.link-with-arrow svg {
  transition: transform 0.3s ease;
}

.link-with-arrow:hover svg {
  transform: translateX(4px);
}

@media (max-width: 768px) {
  .projects-grid {
    grid-template-columns: 1fr;
    gap: var(--spacing-lg);
  }
  
  .project-card-inner {
    padding: var(--spacing-lg);
  }
  
  .project-title {
    font-size: var(--text-xl);
  }
  
  .project-description {
    font-size: var(--text-sm);
  }
}
/*]]>*/
</style>

<script>
//<![CDATA[
document.addEventListener('DOMContentLoaded', function() {
  const projectCards = document.querySelectorAll('.project-card');
  
  projectCards.forEach(card => {
    card.addEventListener('mouseenter', function() {
      this.style.borderColor = 'rgba(59, 130, 246, 0.4)';
    });
    
    card.addEventListener('mouseleave', function() {
      this.style.borderColor = '';
    });
  });
});
//]]>
</script>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- Connect &amp; Socials Section -->
    <b:section class='connect-section section bg-secondary' id='connect' maxwidgets='3' name='Connect &amp; Socials' showaddelement='yes'>
      <!-- Social Links Widget (LinkList) -->
      <b:widget id='LinkList1' locked='false' title='Connect with Me' type='LinkList' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='sorting'>NONE</b:widget-setting>
          <b:widget-setting name='text-0'>LinkedIn</b:widget-setting>
          <b:widget-setting name='link-0'>https://www.linkedin.com/in/abumdselim/</b:widget-setting>
          <b:widget-setting name='text-1'>GitHub</b:widget-setting>
          <b:widget-setting name='link-1'>https://github.com/abumdselim</b:widget-setting>
          <b:widget-setting name='text-2'>Email</b:widget-setting>
          <b:widget-setting name='link-2'>mailto:hello@mishu.dev</b:widget-setting>
        </b:widget-settings>
        
        <b:includable id='main'>
<div class='connect-container'>
  <div class='container'>
    <div class='section-header scroll-reveal'>
      <span class='section-label'>Let's Connect</span>
      <h2 class='section-title headline-premium'>Get in Touch</h2>
      <p class='section-description'>
        Always open to discussing new projects, creative ideas, or opportunities to be part of your vision.
        Let's build something extraordinary together.
      </p>
    </div>
    
    <!-- Bento Grid Social Links -->
    <div class='socials-bento-grid'>
      <b:loop values='data:links' var='link'>
        <b:if cond='data:link.name == &quot;LinkedIn&quot;'>
          <!-- LinkedIn - Primary Bento Card -->
          <a class='social-bento-card bento-large magnetic-card hover-glow scroll-reveal-1' expr:href='data:link.target' target='_blank' rel='noopener noreferrer' data-platform='linkedin'>
            <div class='social-icon-wrapper linkedin-icon'>
              <svg class='social-icon' width='48' height='48' viewBox='0 0 48 48' fill='none' xmlns='http://www.w3.org/2000/svg'>
                <rect width='48' height='48' rx='8' fill='#0A66C2'/>
                <path d='M16 19H12V36H16V19Z' fill='white'/>
                <path d='M14 17C15.1046 17 16 16.1046 16 15C16 13.8954 15.1046 13 14 13C12.8954 13 12 13.8954 12 15C12 16.1046 12.8954 17 14 17Z' fill='white'/>
                <path d='M36 36H32V27.5C32 25.5 31 24 29 24C27 24 26 25.5 26 27.5V36H22V19H26V21C26.5 20 28.5 18 31 18C34.5 18 36 20.5 36 24V36Z' fill='white'/>
              </svg>
            </div>
            <div class='social-content'>
              <h3 class='social-title'>Connect on LinkedIn</h3>
              <p class='social-description'>Professional network &amp; career updates</p>
              <div class='social-cta'>
                <span>Let's connect</span>
                <svg width='20' height='20' viewBox='0 0 20 20' fill='none'>
                  <path d='M7.5 15L12.5 10L7.5 5' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
                </svg>
              </div>
            </div>
          </a>
        </b:if>
        
        <b:if cond='data:link.name == &quot;Email&quot;'>
          <!-- Email - Primary CTA Bento Card -->
          <a class='social-bento-card bento-medium magnetic-card hover-glow scroll-reveal-2' expr:href='data:link.target' data-platform='email'>
            <div class='social-icon-wrapper email-icon'>
              <svg class='social-icon' width='48' height='48' viewBox='0 0 48 48' fill='none' xmlns='http://www.w3.org/2000/svg'>
                <rect width='48' height='48' rx='8' fill='url(#emailGradient)'/>
                <path d='M10 14C10 12.8954 10.8954 12 12 12H36C37.1046 12 38 12.8954 38 14V34C38 35.1046 37.1046 36 36 36H12C10.8954 36 10 35.1046 10 34V14Z' fill='white' fill-opacity='0.2' stroke='white' stroke-width='2'/>
                <path d='M10 14L24 25L38 14' stroke='white' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
                <defs>
                  <linearGradient id='emailGradient' x1='0' y1='0' x2='48' y2='48'>
                    <stop offset='0%' stop-color='#3b82f6'/>
                    <stop offset='100%' stop-color='#8b5cf6'/>
                  </linearGradient>
                </defs>
              </svg>
            </div>
            <div class='social-content'>
              <h3 class='social-title'>Email Me</h3>
              <p class='social-description'>hello@mishu.dev</p>
              <div class='social-cta'>
                <span>Get in touch</span>
                <svg width='20' height='20' viewBox='0 0 20 20' fill='none'>
                  <path d='M7.5 15L12.5 10L7.5 5' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
                </svg>
              </div>
            </div>
          </a>
        </b:if>
        
        <b:if cond='data:link.name == &quot;GitHub&quot;'>
          <!-- GitHub -->
          <a class='social-bento-card bento-small magnetic-card hover-lift scroll-reveal-3' expr:href='data:link.target' target='_blank' rel='noopener noreferrer' data-platform='github'>
            <div class='social-icon-wrapper github-icon'>
              <svg class='social-icon' width='40' height='40' viewBox='0 0 40 40' fill='none' xmlns='http://www.w3.org/2000/svg'>
                <rect width='40' height='40' rx='8' fill='#24292e'/>
                <path d='M20 8C13.37 8 8 13.37 8 20C8 25.31 11.435 29.795 16.205 31.385C16.805 31.49 17.03 31.13 17.03 30.815C17.03 30.53 17.015 29.585 17.015 28.58C14 29.135 13.22 27.845 12.98 27.17C12.845 26.825 12.26 25.76 11.75 25.475C11.33 25.25 10.73 24.695 11.735 24.68C12.68 24.665 13.355 25.55 13.58 25.91C14.66 27.725 16.385 27.215 17.075 26.9C17.18 26.12 17.495 25.595 17.84 25.295C15.17 24.995 12.38 23.96 12.38 19.37C12.38 18.065 12.845 16.985 13.61 16.145C13.49 15.845 13.07 14.615 13.73 12.965C13.73 12.965 14.735 12.65 17.03 14.195C17.99 13.925 19.01 13.79 20.03 13.79C21.05 13.79 22.07 13.925 23.03 14.195C25.325 12.635 26.33 12.965 26.33 12.965C26.99 14.615 26.57 15.845 26.45 16.145C27.215 16.985 27.68 18.05 27.68 19.37C27.68 23.975 24.875 24.995 22.205 25.295C22.64 25.67 23.015 26.39 23.015 27.515C23.015 29.12 23 30.41 23 30.815C23 31.13 23.225 31.505 23.825 31.385C26.205 30.585 28.2575 29.0688 29.6925 27.0575C31.1275 25.0463 31.875 22.6538 31.875 20.2C31.875 13.37 26.63 8 20 8Z' fill='white'/>
              </svg>
            </div>
            <div class='social-content-compact'>
              <h3 class='social-title-compact'>GitHub</h3>
              <p class='social-handle'>@abumdselim</p>
            </div>
          </a>
        </b:if>
        
        <b:if cond='data:link.name == &quot;X&quot; or data:link.name == &quot;Twitter&quot;'>
          <!-- X (Twitter) -->
          <a class='social-bento-card bento-small magnetic-card hover-lift scroll-reveal-4' expr:href='data:link.target' target='_blank' rel='noopener noreferrer' data-platform='x'>
            <div class='social-icon-wrapper x-icon'>
              <svg class='social-icon' width='40' height='40' viewBox='0 0 40 40' fill='none' xmlns='http://www.w3.org/2000/svg'>
                <rect width='40' height='40' rx='8' fill='#000000'/>
                <path d='M23.36 14.5H25.82L21.08 20.02L26.64 28.5H22.48L19.06 23.46L15.14 28.5H12.68L17.74 22.62L12.4 14.5H16.66L19.76 19.16L23.36 14.5ZM22.6 27.18H24.02L16.48 15.94H14.96L22.6 27.18Z' fill='white'/>
              </svg>
            </div>
            <div class='social-content-compact'>
              <h3 class='social-title-compact'>X (Twitter)</h3>
              <p class='social-handle'>Follow</p>
            </div>
          </a>
        </b:if>
        
        <b:if cond='data:link.name == &quot;Facebook&quot;'>
          <!-- Facebook -->
          <a class='social-bento-card bento-small magnetic-card hover-lift scroll-reveal-5' expr:href='data:link.target' target='_blank' rel='noopener noreferrer' data-platform='facebook'>
            <div class='social-icon-wrapper facebook-icon'>
              <svg class='social-icon' width='40' height='40' viewBox='0 0 40 40' fill='none' xmlns='http://www.w3.org/2000/svg'>
                <rect width='40' height='40' rx='8' fill='#1877F2'/>
                <path d='M27.5 20C27.5 15.86 24.14 12.5 20 12.5C15.86 12.5 12.5 15.86 12.5 20C12.5 23.74 15.22 26.83 18.75 27.42V22.5H16.88V20H18.75V18.13C18.75 16.28 19.92 15.19 21.58 15.19C22.38 15.19 23.13 15.31 23.13 15.31V17.13H22.3C21.48 17.13 21.25 17.61 21.25 18.1V20H23.06L22.77 22.5H21.25V27.42C24.78 26.83 27.5 23.74 27.5 20Z' fill='white'/>
              </svg>
            </div>
            <div class='social-content-compact'>
              <h3 class='social-title-compact'>Facebook</h3>
              <p class='social-handle'>Connect</p>
            </div>
          </a>
        </b:if>
        
        <b:if cond='data:link.name == &quot;Instagram&quot;'>
          <!-- Instagram -->
          <a class='social-bento-card bento-small magnetic-card hover-lift scroll-reveal-3' expr:href='data:link.target' target='_blank' rel='noopener noreferrer' data-platform='instagram'>
            <div class='social-icon-wrapper instagram-icon'>
              <svg class='social-icon' width='40' height='40' viewBox='0 0 40 40' fill='none' xmlns='http://www.w3.org/2000/svg'>
                <rect width='40' height='40' rx='8' fill='url(#instagramGradient)'/>
                <rect x='12' y='12' width='16' height='16' rx='4' stroke='white' stroke-width='2' fill='none'/>
                <circle cx='20' cy='20' r='4' stroke='white' stroke-width='2' fill='none'/>
                <circle cx='25.5' cy='14.5' r='1.5' fill='white'/>
                <defs>
                  <linearGradient id='instagramGradient' x1='0' y1='40' x2='40' y2='0'>
                    <stop offset='0%' stop-color='#FD5949'/>
                    <stop offset='50%' stop-color='#D6249F'/>
                    <stop offset='100%' stop-color='#285AEB'/>
                  </linearGradient>
                </defs>
              </svg>
            </div>
            <div class='social-content-compact'>
              <h3 class='social-title-compact'>Instagram</h3>
              <p class='social-handle'>Follow</p>
            </div>
          </a>
        </b:if>
        
        <b:if cond='data:link.name == &quot;Threads&quot;'>
          <!-- Threads -->
          <a class='social-bento-card bento-small magnetic-card hover-lift scroll-reveal-4' expr:href='data:link.target' target='_blank' rel='noopener noreferrer' data-platform='threads'>
            <div class='social-icon-wrapper threads-icon'>
              <svg class='social-icon' width='40' height='40' viewBox='0 0 40 40' fill='none' xmlns='http://www.w3.org/2000/svg'>
                <rect width='40' height='40' rx='8' fill='#000000'/>
                <path d='M22.5 16.5C21.5 15.8 20.3 15.5 19 15.5C15.1 15.5 12 18.6 12 22.5C12 26.4 15.1 29.5 19 29.5C22.9 29.5 26 26.4 26 22.5V20M26 20C26 16.7 23.3 14 20 14C16.7 14 14 16.7 14 20C14 23.3 16.7 26 20 26C23.3 26 26 23.3 26 20Z' stroke='white' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
                <circle cx='20' cy='20' r='2.5' fill='white'/>
              </svg>
            </div>
            <div class='social-content-compact'>
              <h3 class='social-title-compact'>Threads</h3>
              <p class='social-handle'>Follow</p>
            </div>
          </a>
        </b:if>
      </b:loop>
    </div>
    
    <!-- Alternative: Traditional Icon Row (Optional) -->
    <div class='socials-icon-row' style='display: none;'>
      <b:loop values='data:links' var='link'>
        <a class='social-icon-link magnetic-btn' expr:href='data:link.target' expr:title='data:link.name' target='_blank' rel='noopener noreferrer'>
          <b:if cond='data:link.name == &quot;LinkedIn&quot;'>
            <svg width='24' height='24' viewBox='0 0 24 24' fill='currentColor'>
              <path d='M19 3a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h14m-.5 15.5v-5.3a3.26 3.26 0 0 0-3.26-3.26c-.85 0-1.84.52-2.32 1.3v-1.11h-2.79v8.37h2.79v-4.93c0-.77.62-1.4 1.39-1.4a1.4 1.4 0 0 1 1.4 1.4v4.93h2.79M6.88 8.56a1.68 1.68 0 0 0 1.68-1.68c0-.93-.75-1.69-1.68-1.69a1.69 1.69 0 0 0-1.69 1.69c0 .93.76 1.68 1.69 1.68m1.39 9.94v-8.37H5.5v8.37h2.77z'/>
            </svg>
          </b:if>
          <b:if cond='data:link.name == &quot;GitHub&quot;'>
            <svg width='24' height='24' viewBox='0 0 24 24' fill='currentColor'>
              <path d='M12 2A10 10 0 0 0 2 12c0 4.42 2.87 8.17 6.84 9.5.5.08.66-.23.66-.5v-1.69c-2.77.6-3.36-1.34-3.36-1.34-.46-1.16-1.11-1.47-1.11-1.47-.91-.62.07-.6.07-.6 1 .07 1.53 1.03 1.53 1.03.87 1.52 2.34 1.07 2.91.83.09-.65.35-1.09.63-1.34-2.22-.25-4.55-1.11-4.55-4.92 0-1.11.38-2 1.03-2.71-.1-.25-.45-1.29.1-2.64 0 0 .84-.27 2.75 1.02.79-.22 1.65-.33 2.5-.33.85 0 1.71.11 2.5.33 1.91-1.29 2.75-1.02 2.75-1.02.55 1.35.2 2.39.1 2.64.65.71 1.03 1.6 1.03 2.71 0 3.82-2.34 4.66-4.57 4.91.36.31.69.92.69 1.85V21c0 .27.16.59.67.5C19.14 20.16 22 16.42 22 12A10 10 0 0 0 12 2z'/>
            </svg>
          </b:if>
          <b:if cond='data:link.name == &quot;Email&quot;'>
            <svg width='24' height='24' viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='2'>
              <path d='M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z'/>
              <polyline points='22,6 12,13 2,6'/>
            </svg>
          </b:if>
        </a>
      </b:loop>
    </div>
  </div>
</div>

<style>
/*<![CDATA[*/
/* ===========================
   CONNECT & SOCIALS SECTION
   =========================== */

.connect-container {
  padding: var(--spacing-4xl) 0;
  position: relative;
}

.connect-container::before {
  content: '';
  position: absolute;
  top: 20%;
  right: 0;
  width: 500px;
  height: 500px;
  background: radial-gradient(circle, rgba(59, 130, 246, 0.1) 0%, transparent 70%);
  filter: blur(80px);
  pointer-events: none;
  z-index: 0;
}

/* BENTO-GRID SOCIAL LAYOUT */
.socials-bento-grid {
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  gap: var(--spacing-lg);
  margin-bottom: var(--spacing-2xl);
  position: relative;
  z-index: 1;
}

/* Social Bento Cards */
.social-bento-card {
  background: var(--glass-bg);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 20px;
  padding: var(--spacing-xl);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: flex-start;
  gap: var(--spacing-lg);
  text-decoration: none;
  color: var(--text-primary);
}

.social-bento-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: linear-gradient(90deg, transparent, currentColor, transparent);
  opacity: 0;
  transition: opacity 0.4s ease;
}

.social-bento-card:hover::before {
  opacity: 1;
}

.social-bento-card:hover {
  transform: translateY(-8px);
  border-color: rgba(59, 130, 246, 0.4);
  box-shadow: 0 20px 60px rgba(59, 130, 246, 0.3);
}

/* LinkedIn - Primary Large Card */
.social-bento-card[data-platform='linkedin'] {
  grid-column: span 7;
  border-color: rgba(10, 102, 194, 0.3);
}

.social-bento-card[data-platform='linkedin']:hover {
  border-color: rgba(10, 102, 194, 0.6);
  box-shadow: 0 20px 60px rgba(10, 102, 194, 0.4);
}

/* Email - Medium Card */
.social-bento-card[data-platform='email'] {
  grid-column: span 5;
  border-color: rgba(59, 130, 246, 0.3);
}

.social-bento-card[data-platform='email']:hover {
  border-color: rgba(59, 130, 246, 0.6);
  box-shadow: 0 20px 60px rgba(59, 130, 246, 0.4);
}

/* Small Cards - GitHub, X, Facebook, Instagram, Threads */
.social-bento-card[data-platform='github'],
.social-bento-card[data-platform='x'],
.social-bento-card[data-platform='facebook'],
.social-bento-card[data-platform='instagram'],
.social-bento-card[data-platform='threads'] {
  grid-column: span 4;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: var(--spacing-md);
}

/* Icon Wrappers */
.social-icon-wrapper {
  flex-shrink: 0;
  transition: transform 0.3s ease;
}

.social-bento-card:hover .social-icon-wrapper {
  transform: scale(1.1) rotate(-5deg);
}

.social-icon {
  display: block;
}

/* Content for Large Cards (LinkedIn, Email) */
.social-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
}

.social-title {
  font-size: var(--text-2xl);
  font-weight: 700;
  color: var(--text-primary);
  margin: 0;
  font-family: var(--font-primary);
}

.social-description {
  font-size: var(--text-base);
  color: var(--text-secondary);
  margin: 0;
  line-height: 1.6;
}

.social-cta {
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
  font-size: var(--text-base);
  font-weight: 600;
  color: var(--primary-color);
  font-family: var(--font-mono);
  margin-top: var(--spacing-sm);
}

.social-bento-card:hover .social-cta svg {
  transform: translateX(4px);
}

.social-cta svg {
  transition: transform 0.3s ease;
}

/* Content for Small Cards */
.social-content-compact {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs);
  align-items: center;
}

.social-title-compact {
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--text-primary);
  margin: 0;
  font-family: var(--font-primary);
}

.social-handle {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  margin: 0;
  font-family: var(--font-mono);
}

/* Platform-Specific Icon Backgrounds */
.linkedin-icon {
  filter: drop-shadow(0 4px 12px rgba(10, 102, 194, 0.4));
}

.email-icon {
  filter: drop-shadow(0 4px 12px rgba(59, 130, 246, 0.4));
}

.github-icon {
  filter: drop-shadow(0 4px 12px rgba(36, 41, 46, 0.4));
}

.x-icon {
  filter: drop-shadow(0 4px 12px rgba(0, 0, 0, 0.6));
}

.facebook-icon {
  filter: drop-shadow(0 4px 12px rgba(24, 119, 242, 0.4));
}

.instagram-icon {
  filter: drop-shadow(0 4px 12px rgba(214, 36, 159, 0.4));
}

.threads-icon {
  filter: drop-shadow(0 4px 12px rgba(0, 0, 0, 0.6));
}

/* ALTERNATIVE: TRADITIONAL ICON ROW */
.socials-icon-row {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: var(--spacing-lg);
  flex-wrap: wrap;
}

.social-icon-link {
  width: 56px;
  height: 56px;
  border-radius: 12px;
  background: var(--glass-bg);
  border: 1px solid rgba(255, 255, 255, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-primary);
  transition: all 0.3s ease;
  text-decoration: none;
}

.social-icon-link:hover {
  transform: translateY(-4px) scale(1.05);
  border-color: var(--primary-color);
  background: rgba(59, 130, 246, 0.1);
  box-shadow: 0 8px 24px rgba(59, 130, 246, 0.3);
}

/* RESPONSIVE DESIGN */
@media (max-width: 1024px) {
  .social-bento-card[data-platform='linkedin'],
  .social-bento-card[data-platform='email'] {
    grid-column: span 12;
  }
  
  .social-bento-card[data-platform='github'],
  .social-bento-card[data-platform='x'],
  .social-bento-card[data-platform='facebook'],
  .social-bento-card[data-platform='instagram'],
  .social-bento-card[data-platform='threads'] {
    grid-column: span 6;
  }
}

@media (max-width: 768px) {
  .socials-bento-grid {
    grid-template-columns: 1fr;
    gap: var(--spacing-md);
  }
  
  .social-bento-card {
    grid-column: 1 / -1 !important;
    flex-direction: row;
    text-align: left;
    align-items: center;
  }
  
  .social-bento-card[data-platform='github'],
  .social-bento-card[data-platform='x'],
  .social-bento-card[data-platform='facebook'],
  .social-bento-card[data-platform='instagram'],
  .social-bento-card[data-platform='threads'] {
    flex-direction: row;
    text-align: left;
  }
  
  .social-content-compact {
    align-items: flex-start;
  }
  
  .social-icon-wrapper {
    width: 48px;
    height: 48px;
  }
  
  .social-icon {
    width: 48px;
    height: 48px;
  }
}

@media (max-width: 480px) {
  .connect-container {
    padding: var(--spacing-3xl) 0;
  }
  
  .social-title {
    font-size: var(--text-xl);
  }
  
  .social-icon-wrapper {
    width: 40px;
    height: 40px;
  }
  
  .social-icon {
    width: 40px;
    height: 40px;
  }
}
/*]]>*/
</style>

<script>
//<![CDATA[
document.addEventListener('DOMContentLoaded', function() {
  // Add magnetic effect to social cards
  const socialCards = document.querySelectorAll('.social-bento-card');
  
  socialCards.forEach(card => {
    card.addEventListener('mouseenter', function() {
      this.style.borderColor = getComputedStyle(this).getPropertyValue('--hover-color') || 'rgba(59, 130, 246, 0.6)';
    });
    
    card.addEventListener('mouseleave', function() {
      this.style.borderColor = '';
    });
  });
  
  // Track social link clicks (optional analytics)
  socialCards.forEach(card => {
    card.addEventListener('click', function() {
      const platform = this.getAttribute('data-platform');
      console.log('Social link clicked:', platform);
      // Add your analytics tracking here if needed
    });
  });
});
//]]>
</script>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- Pre-Footer CTA Section -->
    <b:section class='pre-footer-section section' id='pre-footer' maxwidgets='2' name='Pre-Footer CTA' showaddelement='yes'>
      <b:widget id='HTML98' locked='false' title='Hire Me CTA' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<div class="pre-footer-cta-container">
  <div class="pre-footer-cta-card bento-large">
    <div class="cta-content">
      <div class="cta-badge">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
          <polyline points="7 10 12 15 17 10"/>
          <line x1="12" y1="15" x2="12" y2="3"/>
        </svg>
        <span>Available for Work</span>
      </div>
      <h2 class="cta-headline">Have a Project in Mind?</h2>
      <p class="cta-description">Let's build something amazing together. I specialize in full-stack development, AI integration, and creating scalable digital products.</p>
      <div class="cta-actions">
        <a href="#connect" class="cta-primary-btn magnetic-btn">Hire Me</a>
        <a href="#projects" class="cta-secondary-btn">View My Work</a>
      </div>
    </div>
    <div class="cta-decoration">
      <div class="cta-orb cta-orb-1"></div>
      <div class="cta-orb cta-orb-2"></div>
    </div>
  </div>
</div>

<style>
/*<![CDATA[*/
/* Pre-Footer CTA Section */
.pre-footer-section {
  padding: var(--spacing-4xl) 0;
  background: var(--bg-primary);
  position: relative;
  overflow: hidden;
}

.pre-footer-cta-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 var(--spacing-lg);
}

.pre-footer-cta-card {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(139, 92, 246, 0.2);
  border-radius: var(--border-radius-xl);
  padding: var(--spacing-3xl);
  position: relative;
  overflow: hidden;
  transition: var(--transition);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

.pre-footer-cta-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent 0%, rgba(139, 92, 246, 0.5) 50%, transparent 100%);
}

.pre-footer-cta-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 16px 48px rgba(139, 92, 246, 0.2);
  border-color: rgba(139, 92, 246, 0.4);
}

.cta-content {
  position: relative;
  z-index: 2;
  max-width: 700px;
}

.cta-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  background: rgba(34, 197, 94, 0.1);
  border: 1px solid rgba(34, 197, 94, 0.3);
  border-radius: var(--border-radius-md);
  color: #22c55e;
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: var(--spacing-lg);
  animation: pulse-glow 3s ease-in-out infinite;
}

.cta-badge svg {
  width: 20px;
  height: 20px;
}

.cta-headline {
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 800;
  line-height: 1.1;
  margin-bottom: var(--spacing-md);
  background: linear-gradient(135deg, #ffffff 0%, #3b82f6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.cta-description {
  font-size: 1.125rem;
  line-height: 1.6;
  color: var(--text-secondary);
  margin-bottom: var(--spacing-xl);
  max-width: 600px;
}

.cta-actions {
  display: flex;
  gap: var(--spacing-md);
  flex-wrap: wrap;
}

.cta-primary-btn {
  padding: 14px 32px;
  border-radius: var(--border-radius-lg);
  background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
  border: 1px solid rgba(139, 92, 246, 0.3);
  color: #ffffff;
  font-size in: 1rem;
  font-weight: 700;
  text-decoration: none;
  position: relative;
  overflow: hidden;
  transition: var(--transition);
  box-shadow: 0 4px 20px rgba(139, 92, 246, 0.4);
  z-index: 1;
}

.cta-primary-btn::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.2) 0%, transparent 70%);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  transition: width 0.6s ease, height 0.6s ease;
  z-index: -1;
}

.cta-primary-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 40px rgba(139, 92, 246, 0.6);
}

.cta-primary-btn:hover::before {
  width: 300px;
  height: 300px;
}

.cta-secondary-btn {
  padding: 14px 32px;
  border-radius: var(--border-radius-lg);
  background: rgba(59, 130, 246, 0.1);
  border: 1px solid rgba(59, 130, 246, 0.3);
  color: var(--primary-color);
  font-size: 1rem;
  font-weight: 600;
  text-decoration: none;
  transition: var(--transition);
}

.cta-secondary-btn:hover {
  background: rgba(59, 130, 246, 0.2);
  border-color: rgba(59, 130, 246, 0.5);
  transform: translateY(-2px);
}

/* CTA Decorative Orbs */
.cta-decoration {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 1;
}

.cta-orb {
  position: absolute;
  border-radius: 50%;
  filter: blur(80px);
  opacity: 0.3;
  animation: cta-float 15s ease-in-out infinite;
}

.cta-orb-1 {
  width: 400px;
  height: 400px;
  background: radial-gradient(circle, #3b82f6 0%, transparent 70%);
  top: -200px;
  right: -100px;
}

.cta-orb-2 {
  width: 300px;
  height: 300px;
  background: radial-gradient(circle, #8b5cf6 0%, transparent 70%);
  bottom: -150px;
  left: -50px;
  animation-delay: -5s;
}

@keyframes cta-float {
  0%, 100% {
    transform: translate(0, 0) scale(1);
  }
  33% {
    transform: translate(30px, -30px) scale(1.1);
  }
  66% {
    transform: translate(-20px, 20px) scale(0.9);
  }
}

/* Responsive */
@media (max-width: 768px) {
  .pre-footer-section {
    padding: var(--spacing-2xl) 0;
  }
  
  .pre-footer-cta-card {
    padding: var(--spacing-xl);
  }
  
  .cta-headline {
    font-size: 2rem;
  }
  
  .cta-description {
    font-size: 1rem;
  }
  
  .cta-actions {
    flex-direction: column;
  }
  
  .cta-primary-btn,
  .cta-secondary-btn {
    width: 100%;
    text-align: center;
  }
  
  .cta-orb-1,
  .cta-orb-2 {
    width: 250px;
    height: 250px;
  }
}
/*]]>*/
</style>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- Footer Section -->
    <b:section class='footer-section section' id='footer' maxwidgets='4' name='Footer' showaddelement='yes' style='background: var(--bg-tertiary); border-top: 1px solid var(--border-color);'>
      <b:widget id='HTML100' locked='false' title='Footer Content' type='HTML' version='2' visible='true'>
        <b:widget-settings>
          <b:widget-setting name='content'><![CDATA[
<div class="footer-container">
  <div class="container">
    <div class="footer-content">
      <div class="footer-brand">
        <h3 class="footer-logo">mishu.dev</h3>
        <p class="footer-tagline">Building the future, one venture at a time.</p>
      </div>
      
      <div class="footer-links">
        <div class="footer-column">
          <h4 class="footer-heading">Ventures</h4>
          <ul class="footer-list">
            <li><a href="#projects">Intactic Systems</a></li>
            <li><a href="#projects">The Chattala</a></li>
            <li><a href="#projects">Portfolio</a></li>
          </ul>
        </div>
        
        <div class="footer-column">
          <h4 class="footer-heading">Resources</h4>
          <ul class="footer-list">
            <li><a href="#blog">Blog</a></li>
            <li><a href="#workflow">AI Stack</a></li>
            <li><a href="#about">About</a></li>
          </ul>
        </div>
        
        <div class="footer-column">
          <h4 class="footer-heading">Connect</h4>
          <ul class="footer-list">
            <li><a href="#connect">LinkedIn</a></li>
            <li><a href="#connect">GitHub</a></li>
            <li><a href="#connect">Email</a></li>
          </ul>
        </div>
      </div>
    </div>
    
    <div class="footer-bottom">
      <p class="footer-copyright">
        © <span id="currentYear">2024</span> Abu Md Selim (Mishu). All rights reserved.
      </p>
      <p class="footer-credits">
        Crafted with <span class="footer-heart">❤️</span> and cutting-edge tech
      </p>
    </div>
  </div>
</div>

<style>
/*<![CDATA[*/
/* Footer Styles */
.footer-container {
  padding: var(--spacing-3xl) 0 var(--spacing-xl);
}

.footer-content {
  display: grid;
  grid-template-columns: 1.5fr 1fr;
  gap: var(--spacing-2xl);
  margin-bottom: var(--spacing-2xl);
  padding-bottom: var(--spacing-2xl);
  border-bottom: 1px solid var(--border-color);
}

.footer-brand {
  max-width: 400px;
}

.footer-logo {
  font-size: 1.5rem;
  font-weight: 800;
  background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: var(--spacing-sm);
}

.footer-tagline {
  color: var(--text-secondary);
  line-height: 1.6;
}

.footer-links {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--spacing-xl);
}

.footer-column {
  min-width: 0;
}

.footer-heading {
  font-size: 0.875rem;
  font-weight: 700;
  color: var(--text-primary);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: var(--spacing-md);
}

.footer-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
}

.footer-list a {
  color: var(--text-secondary);
  text-decoration: none;
  transition: var(--transition);
  display: inline-block;
}

.footer-list a:hover {
  color: var(--primary-color);
  transform: translateX(4px);
}

.footer-bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: var(--spacing-md);
  padding-top: var(--spacing-lg);
}

.footer-copyright,
.footer-credits {
  color: var(--text-muted);
  font-size: 0.875rem;
}

.footer-heart {
  color: #ef4444;
  animation: heartbeat 2s ease-in-out infinite;
}

@keyframes heartbeat {
  0%, 100% {
    transform: scale(1);
  }
  15% {
    transform: scale(1.2);
  }
  30% {
    transform: scale(1);
  }
}

/* Responsive */
@media (max-width: 768px) {
  .footer-content {
    grid-template-columns: 1fr;
    gap: var(--spacing-xl);
  }
  
  .footer-links {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .footer-bottom {
    flex-direction: column;
    text-align: center;
  }
}

@media (max-width: 480px) {
  .footer-links {
    grid-template-columns: 1fr;
  }
}
/*]]>*/
</style>

<script>
//<![CDATA[
// Auto-update copyright year
document.getElementById('currentYear').textContent = new Date().getFullYear();
//]]>
</script>
]]></b:widget-setting>
        </b:widget-settings>
        <b:includable id='main'>
          <data:content/>
        </b:includable>
      </b:widget>
    </b:section>
    
    <!-- Reading Time Calculator Script -->
    <script>
    //<![CDATA[
    (function() {
      'use strict';
      
      // Configuration
      const WORDS_PER_MINUTE = 200; // Average reading speed
      const MIN_READ_TIME = 1; // Minimum reading time in minutes
      
      /**
       * Calculate reading time based on word count
       * @param {string} text - The text content to analyze
       * @returns {number} - Estimated reading time in minutes
       */
      function calculateReadingTime(text) {
        if (!text || typeof text !== 'string') return MIN_READ_TIME;
        
        // Remove extra whitespace and split into words
        const words = text.trim().split(/\s+/).filter(word => word.length > 0);
        const wordCount = words.length;
        
        // Calculate reading time (round up)
        const minutes = Math.ceil(wordCount / WORDS_PER_MINUTE);
        
        // Return at least minimum reading time
        return Math.max(minutes, MIN_READ_TIME);
      }
      
      /**
       * Format reading time for display
       * @param {number} minutes - Reading time in minutes
       * @returns {string} - Formatted reading time text
       */
      function formatReadingTime(minutes) {
        if (minutes === 1) {
          return '⏳ 1 min read';
        }
        return `⏳ ${minutes} min read`;
      }
      
      /**
       * Extract text content from post body
       * @param {string} postUrl - URL of the post
       * @returns {Promise<string>} - Post text content
       */
      async function fetchPostContent(postUrl) {
        try {
          const response = await fetch(postUrl);
          if (!response.ok) throw new Error('Failed to fetch post');
          
          const html = await response.text();
          const parser = new DOMParser();
          const doc = parser.parseFromString(html, 'text/html');
          
          // Find post body - try common Blogger selectors
          const postBody = doc.querySelector('.post-body, [itemprop="articleBody"], .entry-content, article .post-content');
          
          if (!postBody) {
            console.warn('Post body not found for:', postUrl);
            return '';
          }
          
          // Get text content, excluding scripts and styles
          const scripts = postBody.querySelectorAll('script, style');
          scripts.forEach(el => el.remove());
          
          return postBody.textContent || postBody.innerText || '';
          
        } catch (error) {
          console.error('Error fetching post content:', error);
          return '';
        }
      }
      
      /**
       * Update reading time display for a post card
       * @param {Element} readingTimeElement - The reading time display element
       * @param {string} postUrl - URL of the post
       */
      async function updateReadingTime(readingTimeElement, postUrl) {
        const textSpan = readingTimeElement.querySelector('.reading-time-text');
        if (!textSpan) return;
        
        try {
          // For post list pages, use snippet as approximation
          const postCard = readingTimeElement.closest('.post-card');
          let textContent = '';
          
          if (postCard) {
            // Try to get snippet text
            const snippetElement = postCard.querySelector('.post-snippet');
            if (snippetElement) {
              textContent = snippetElement.textContent || '';
              // Approximate full post length based on snippet (assuming snippet is ~10% of full post)
              const approximateWords = textContent.trim().split(/\s+/).length * 10;
              const minutes = Math.ceil(approximateWords / WORDS_PER_MINUTE);
              textSpan.textContent = formatReadingTime(Math.max(minutes, MIN_READ_TIME));
              return;
            }
          }
          
          // If on individual post page, fetch full content
          const content = await fetchPostContent(postUrl);
          const readingTime = calculateReadingTime(content);
          textSpan.textContent = formatReadingTime(readingTime);
          
        } catch (error) {
          console.error('Error updating reading time:', error);
          textSpan.textContent = '⏳ 5 min read'; // Fallback
        }
      }
      
      /**
       * Initialize reading time calculations for all posts
       */
      function initReadingTime() {
        // Find all reading time elements
        const readingTimeElements = document.querySelectorAll('.post-reading-time');
        
        readingTimeElements.forEach(element => {
          const postId = element.getAttribute('data-post-id');
          
          // Get post URL from the post card
          const postCard = element.closest('.post-card');
          if (!postCard) return;
          
          const postLink = postCard.querySelector('.post-title a, .read-more-btn');
          if (!postLink) return;
          
          const postUrl = postLink.getAttribute('href');
          if (!postUrl) return;
          
          // Update reading time
          updateReadingTime(element, postUrl);
        });
        
        // Handle individual post pages (if post body is on the same page)
        const postBody = document.querySelector('.post-body, [itemprop="articleBody"], .entry-content');
        if (postBody) {
          const textContent = postBody.textContent || postBody.innerText || '';
          const readingTime = calculateReadingTime(textContent);
          
          // Update all reading time displays on the page with actual calculation
          readingTimeElements.forEach(element => {
            const textSpan = element.querySelector('.reading-time-text');
            if (textSpan) {
              textSpan.textContent = formatReadingTime(readingTime);
            }
          });
        }
      }
      
      // Initialize on DOM ready
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initReadingTime);
      } else {
        initReadingTime();
      }
      
      // Re-initialize if content is dynamically loaded
      window.addEventListener('load', initReadingTime);
      
    })();
    //]]>
    </script>
    
    <!-- Table of Contents Generator Script -->
    <script>
    //<![CDATA[
    (function() {
      'use strict';
      
      /**
       * Generate a URL-friendly slug from text
       */
      function slugify(text) {
        return text
          .toString()
          .toLowerCase()
          .trim()
          .replace(/\s+/g, '-')           // Replace spaces with -
          .replace(/[^\w\-]+/g, '')       // Remove non-word chars
          .replace(/\-\-+/g, '-')         // Replace multiple - with single -
          .replace(/^-+/, '')              // Trim - from start
          .replace(/-+$/, '');             // Trim - from end
      }
      
      /**
       * Check if current page is a single post page
       */
      function isSinglePostPage() {
        // Check if body has item view class or if we can find a single post container
        const body = document.body;
        const hasItemView = body.classList.contains('item-view') || 
                           body.classList.contains('post-view') ||
                           body.querySelector('.post-body, [itemprop="articleBody"], .entry-content, article .content');
        
        // Also check URL pattern - Blogger single posts typically have .html extension
        const url = window.location.pathname;
        const isSinglePost = url.includes('.html') || body.classList.contains('item');
        
        return hasItemView && isSinglePost;
      }
      
      /**
       * Find the main post content container
       */
      function findPostContent() {
        const selectors = [
          '.post-body',
          '[itemprop="articleBody"]',
          '.entry-content',
          'article .content',
          '.post-content',
          'article',
          '.blog-post',
          'main article'
        ];
        
        for (let selector of selectors) {
          const content = document.querySelector(selector);
          if (content && content.querySelector('h2, h3')) {
            return content;
          }
        }
        
        return null;
      }
      
      /**
       * Extract headings from post content
       */
      function extractHeadings(container) {
        const headings = container.querySelectorAll('h2, h3');
        const headingData = [];
        const usedIds = new Set();
        
        headings.forEach((heading, index) => {
          const level = heading.tagName.toLowerCase();
          const text = heading.textContent.trim();
          
          if (!text) return; // Skip empty headings
          
          // Generate or use existing ID
          let id = heading.id;
          if (!id) {
            id = slugify(text);
            // Ensure unique IDs
            let uniqueId = id;
            let counter = 1;
            while (usedIds.has(uniqueId)) {
              uniqueId = `${id}-${counter}`;
              counter++;
            }
            id = uniqueId;
            heading.id = id;
          }
          usedIds.add(id);
          
          headingData.push({
            id: id,
            text: text,
            level: level,
            element: heading
          });
        });
        
        return headingData;
      }
      
      /**
       * Build nested TOC structure
       */
      function buildTOCStructure(headings) {
        const structure = [];
        let currentH2 = null;
        
        headings.forEach(heading => {
          if (heading.level === 'h2') {
            currentH2 = {
              ...heading,
              children: []
            };
            structure.push(currentH2);
          } else if (heading.level === 'h3' && currentH2) {
            currentH2.children.push(heading);
          }
        });
        
        return structure;
      }
      
      /**
       * Generate TOC HTML
       */
      function generateTOCHTML(structure) {
        if (!structure.length) {
          return '<p class="toc-empty">No headings found</p>';
        }
        
        let html = '<ul class="toc-list">';
        
        structure.forEach(h2 => {
          html += `
            <li class="toc-item">
              <a href="#${h2.id}" class="toc-link" data-target="${h2.id}">
                ${h2.text}
              </a>`;
          
          if (h2.children.length > 0) {
            html += '<ul class="toc-list">';
            h2.children.forEach(h3 => {
              html += `
                <li class="toc-item">
                  <a href="#${h3.id}" class="toc-link" data-target="${h3.id}">
                    ${h3.text}
                  </a>
                </li>`;
            });
            html += '</ul>';
          }
          
          html += '</li>';
        });
        
        html += '</ul>';
        return html;
      }
      
      /**
       * Create TOC container element
       */
      function createTOCContainer() {
        const toc = document.createElement('aside');
        toc.className = 'toc-container';
        toc.innerHTML = `
          <div class="toc-header">
            <svg class="toc-icon" width="20" height="20" viewBox="0 0 24 24" fill="none">
              <path d="M3 6h18M3 12h18M3 18h18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
            <h2 class="toc-title">Contents</h2>
          </div>
          <nav class="toc-nav"></nav>
        `;
        return toc;
      }
      
      /**
       * Smooth scroll to target
       */
      function smoothScrollTo(targetId) {
        const target = document.getElementById(targetId);
        if (target) {
          const offset = 100; // Account for fixed header
          const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - offset;
          
          window.scrollTo({
            top: targetPosition,
            behavior: 'smooth'
          });
        }
      }
      
      /**
       * Highlight active section based on scroll position
       */
      function updateActiveSection() {
        const tocLinks = document.querySelectorAll('.toc-link');
        if (!tocLinks.length) return;
        
        const headings = Array.from(tocLinks).map(link => {
          const targetId = link.getAttribute('data-target');
          return document.getElementById(targetId);
        }).filter(Boolean);
        
        if (!headings.length) return;
        
        // Find the current active heading based on scroll position
        const scrollPosition = window.scrollY + 150; // Offset for header
        let activeHeading = null;
        
        for (let i = headings.length - 1; i >= 0; i--) {
          const heading = headings[i];
          if (heading.offsetTop <= scrollPosition) {
            activeHeading = heading;
            break;
          }
        }
        
        // Update active states
        tocLinks.forEach(link => {
          link.classList.remove('active');
          if (activeHeading && link.getAttribute('data-target') === activeHeading.id) {
            link.classList.add('active');
          }
        });
      }
      
      /**
       * Throttle function for scroll performance
       */
      function throttle(func, wait) {
        let timeout;
        return function() {
          if (!timeout) {
            timeout = setTimeout(() => {
              func();
              timeout = null;
            }, wait);
          }
        };
      }
      
      /**
       * Initialize Table of Contents
       */
      function initTableOfContents() {
        // Only run on single post pages
        if (!isSinglePostPage()) {
          return;
        }
        
        // Find post content
        const postContent = findPostContent();
        if (!postContent) {
          console.log('TOC: No post content found');
          return;
        }
        
        // Extract headings
        const headings = extractHeadings(postContent);
        if (!headings.length) {
          console.log('TOC: No headings found in post');
          return;
        }
        
        console.log(`TOC: Found ${headings.length} headings`);
        
        // Build structure
        const structure = buildTOCStructure(headings);
        
        // Create TOC container
        const tocContainer = createTOCContainer();
        const tocNav = tocContainer.querySelector('.toc-nav');
        tocNav.innerHTML = generateTOCHTML(structure);
        
        // Append to body
        document.body.appendChild(tocContainer);
        
        // Add click handlers for smooth scrolling
        const tocLinks = tocContainer.querySelectorAll('.toc-link');
        tocLinks.forEach(link => {
          link.addEventListener('click', (e) => {
            e.preventDefault();
            const targetId = link.getAttribute('data-target');
            smoothScrollTo(targetId);
            
            // Update URL hash
            if (history.pushState) {
              history.pushState(null, null, `#${targetId}`);
            } else {
              window.location.hash = targetId;
            }
          });
        });
        
        // Add scroll listener for active highlighting
        const throttledUpdate = throttle(updateActiveSection, 100);
        window.addEventListener('scroll', throttledUpdate);
        
        // Initial active section update
        updateActiveSection();
        
        // Handle initial hash in URL
        if (window.location.hash) {
          setTimeout(() => {
            const targetId = window.location.hash.substring(1);
            smoothScrollTo(targetId);
          }, 100);
        }
      }
      
      // Initialize when DOM is ready
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initTableOfContents);
      } else {
        initTableOfContents();
      }
      
      // Also try on window load in case content loads late
      window.addEventListener('load', () => {
        if (!document.querySelector('.toc-container')) {
          initTableOfContents();
        }
      });
      
    })();
    //]]>
    </script>
    
    <!-- AJAX Load More Posts Script -->
    <script>
    //<![CDATA[
    (function() {
      'use strict';
      
      /**
       * Initialize Load More functionality
       */
      function initLoadMore() {
        const loadMoreBtn = document.getElementById('load-more-posts');
        
        if (!loadMoreBtn) {
          console.log('Load More: Button not found (no more pages)');
          return;
        }
        
        const postsGrid = document.querySelector('.posts-grid');
        
        if (!postsGrid) {
          console.error('Load More: Posts grid container not found');
          return;
        }
        
        let isLoading = false;
        let currentPage = 1;
        
        // Get initial next URL from button data attribute
        let nextPageUrl = loadMoreBtn.getAttribute('data-next-url');
        
        /**
         * Fetch next page of posts via AJAX
         */
        async function loadMorePosts() {
          if (isLoading || !nextPageUrl) {
            return;
          }
          
          isLoading = true;
          
          // Update button to loading state
          loadMoreBtn.classList.add('loading');
          loadMoreBtn.disabled = true;
          
          try {
            console.log(`Load More: Fetching ${nextPageUrl}`);
            
            // Fetch the next page
            const response = await fetch(nextPageUrl);
            
            if (!response.ok) {
              throw new Error(`HTTP error! status: ${response.status}`);
            }
            
            const html = await response.text();
            
            // Parse the HTML response
            const parser = new DOMParser();
            const doc = parser.parseFromString(html, 'text/html');
            
            // Extract new post cards
            const newPosts = doc.querySelectorAll('.post-card');
            
            if (newPosts.length === 0) {
              console.log('Load More: No posts found in response');
              showEndMessage();
              return;
            }
            
            console.log(`Load More: Found ${newPosts.length} new posts`);
            
            // Add fade-in class and append posts with staggered animation
            newPosts.forEach((post, index) => {
              post.classList.add('ajax-loaded');
              post.style.animationDelay = `${index * 0.1}s`;
              postsGrid.appendChild(post.cloneNode(true));
            });
            
            // Update next page URL
            const nextLoadMoreBtn = doc.getElementById('load-more-posts');
            if (nextLoadMoreBtn) {
              nextPageUrl = nextLoadMoreBtn.getAttribute('data-next-url');
              loadMoreBtn.setAttribute('data-next-url', nextPageUrl);
              console.log(`Load More: Next URL updated to ${nextPageUrl}`);
            } else {
              // No more pages
              nextPageUrl = null;
              console.log('Load More: No more pages available');
              showEndMessage();
              return;
            }
            
            // Update page counter
            currentPage++;
            
            // Re-initialize reading time for new posts
            if (typeof initReadingTime === 'function') {
              setTimeout(() => {
                initReadingTime();
              }, 100);
            }
            
            // Scroll to first new post smoothly
            setTimeout(() => {
              const firstNewPost = postsGrid.querySelector('.post-card.ajax-loaded');
              if (firstNewPost) {
                const offset = 120; // Header height
                const targetPosition = firstNewPost.getBoundingClientRect().top + window.pageYOffset - offset;
                
                window.scrollTo({
                  top: targetPosition,
                  behavior: 'smooth'
                });
              }
            }, 200);
            
          } catch (error) {
            console.error('Load More: Error loading posts', error);
            showErrorMessage(error.message);
          } finally {
            isLoading = false;
            loadMoreBtn.classList.remove('loading');
            loadMoreBtn.disabled = false;
          }
        }
        
        /**
         * Show end of posts message
         */
        function showEndMessage() {
          loadMoreBtn.disabled = true;
          loadMoreBtn.classList.add('loading');
          
          const btnText = loadMoreBtn.querySelector('.btn-text');
          if (btnText) {
            btnText.textContent = 'No More Posts';
          }
          
          const icon = loadMoreBtn.querySelector('.btn-icon');
          if (icon) {
            icon.innerHTML = '<circle cx="10" cy="10" r="1.5" fill="currentColor"/><circle cx="6" cy="10" r="1.5" fill="currentColor"/><circle cx="14" cy="10" r="1.5" fill="currentColor"/>';
          }
          
          // Hide button after delay
          setTimeout(() => {
            loadMoreBtn.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
            loadMoreBtn.style.opacity = '0';
            loadMoreBtn.style.transform = 'scale(0.9)';
            
            setTimeout(() => {
              const container = loadMoreBtn.closest('.load-more-container');
              if (container) {
                container.innerHTML = '<div class="load-more-message">🎉 You\'ve reached the end! That\'s all the posts.</div>';
              }
            }, 500);
          }, 1500);
        }
        
        /**
         * Show error message
         */
        function showErrorMessage(message) {
          const btnText = loadMoreBtn.querySelector('.btn-text');
          if (btnText) {
            btnText.textContent = 'Error Loading';
          }
          
          // Show error notification
          const container = loadMoreBtn.closest('.load-more-container');
          if (container) {
            const errorDiv = document.createElement('div');
            errorDiv.className = 'load-more-message';
            errorDiv.style.color = '#ef4444';
            errorDiv.textContent = `⚠️ Failed to load posts. ${message}`;
            container.appendChild(errorDiv);
            
            // Remove error after 5 seconds
            setTimeout(() => {
              errorDiv.remove();
              const btnText = loadMoreBtn.querySelector('.btn-text');
              if (btnText) {
                btnText.textContent = 'Try Again';
              }
            }, 5000);
          }
        }
        
        // Add click event listener
        loadMoreBtn.addEventListener('click', loadMorePosts);
        
        // Optional: Auto-load when scrolling near bottom (infinite scroll)
        // Uncomment to enable
        /*
        let autoLoadEnabled = true;
        
        function checkAutoLoad() {
          if (!autoLoadEnabled || isLoading || !nextPageUrl) return;
          
          const btnPosition = loadMoreBtn.getBoundingClientRect();
          const windowHeight = window.innerHeight;
          
          // Trigger when button is 300px from viewport
          if (btnPosition.top < windowHeight + 300) {
            autoLoadEnabled = false; // Prevent multiple triggers
            loadMorePosts().then(() => {
              autoLoadEnabled = true;
            });
          }
        }
        
        window.addEventListener('scroll', throttle(checkAutoLoad, 200));
        */
        
        console.log('Load More: Initialized successfully');
      }
      
      /**
       * Throttle function for performance
       */
      function throttle(func, wait) {
        let timeout;
        return function() {
          if (!timeout) {
            timeout = setTimeout(() => {
              func();
              timeout = null;
            }, wait);
          }
        };
      }
      
      // Initialize when DOM is ready
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initLoadMore);
      } else {
        initLoadMore();
      }
      
      // Also try on window load
      window.addEventListener('load', () => {
        if (!document.getElementById('load-more-posts')?.dataset.initialized) {
          initLoadMore();
        }
      });
      
    })();
    //]]>
    </script>
  </body>
</html>
